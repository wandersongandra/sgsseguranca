# SGS — log de execução do hardening do módulo PT

**Worktree:** `C:\Users\User\Documents\trae_projects\sgs-pt-hardening-20260910`
**Branch:** `audit/pt-module-hardening-20260910`
**Base:** `5a5ae66ae8eb6410ade8a4b0e68dbd3105fc6219`

## GATE 0 — SETUP / BASELINE

- **Comando ou inspeção:** `git status --short --branch`; `git rev-parse HEAD`; inspeção da worktree criada a partir de `origin/main`.
- **Evidência:** branch isolada em `audit/pt-module-hardening-20260910`, base `5a5ae66ae8eb6410ade8a4b0e68dbd3105fc6219`. A worktree já contém apenas as diferenças EOL conhecidas em `frontend/app/verify/page.tsx` e `frontend/app/verify/page.test.tsx`; `git diff --ignore-space-at-eol` não mostrou diferença funcional.
- **Classificação:** `PASS` de isolamento; `EXCLUDED_PREEXISTING` para os dois arquivos `verify`.
- **Ação tomada:** nenhuma alteração funcional; arquivos `verify` não serão adicionados nem normalizados nesta frente.

## GATE 1 — INVENTÁRIO PT

- **Comando ou inspeção:** leitura de `backend/src/modules/pts/**`, `frontend/app/dashboard/pts/**`, `frontend/src/services/ptsService.ts`, migrations PT/RLS e testes existentes; busca por rotas, tenant, storage, status, `deleted_at`, `pt_executantes`, `window.confirm`, `createObjectURL` e labels.
- **Evidência:** inventário e achados detalhados em `docs/audits/pt-module-hardening-plan-20260910.md`. Backend tem controller, service, entidade, DTOs, migrations e suítes dedicadas; frontend tem páginas `new`/`edit`, `usePts`, `PtForm`, listagem, modais, anexos e PDF.
- **Classificação:** `IN_PROGRESS`; garantias live de banco/RLS/storage ainda `NÃO VERIFICADAS`.
- **Ação tomada:** nenhuma correção antes do baseline executável.

## GATE 0.1 — BASELINE EXECUTÁVEL

- **Comando ou inspeção:** em `backend/`: `npm ci`, `npm run lint`, `npm run type-check`, `npm run test:ci`, `npm run build`, `npm audit --audit-level=high`; em `frontend/`: os mesmos comandos, usando `npx tsc --noEmit --pretty false` no typecheck.
- **Evidência:** `npm ci` terminou com `found 0 vulnerabilities` em ambas as subárvores. Backend: lint exit 0, typecheck exit 0, `325 passed` suítes / `2839 passed` testes, build exit 0, audit exit 0 com `found 0 vulnerabilities`. Frontend: lint exit 0, typecheck exit 0, `161 passed` suítes de `162` (1 skipped), `893 passed` de `895` testes (2 skipped), audit exit 0 com `found 0 vulnerabilities`. O build frontend exit 1 antes do Next iniciar: `[env] NEXT_PUBLIC_API_URL é obrigatória para build/start protegidos`.
- **Classificação:** `PASS` nos checks executáveis; `BLOCKED` somente no build frontend por configuração pública obrigatória ausente; não é falha de código PT.
- **Ação tomada:** nenhum valor de ambiente inventado e nenhuma alteração funcional antes do registro. O bloqueio permanece explícito para a validação de build/browser.

## GATE 1.1 — REPRODUÇÃO PRÉ-CORREÇÃO

- **Comando ou inspeção:** teste focused a ser executado após adicionar somente as asserções/regressões do gate, antes da implementação; leitura comparativa do código em `backend/src/modules/pts/pts.service.ts:2280-2300`, `frontend/app/dashboard/pts/hooks/usePts.ts:491-827` e `frontend/app/dashboard/pts/components/ChecklistSection.tsx:180-203`.
- **Evidência:** baseline acima verde não cobria os três invariantes. O teste de analytics ainda não exige `deleted_at`; não há teste do hook contra duas chamadas antes da resolução; o grupo de rádio não possui `fieldset`/`legend` nem descrição associada.
- **Classificação:** `CONFIRMED` para as lacunas de cobertura; regressões serão demonstradas com testes vermelhos antes da correção.
- **Ação tomada:** nenhuma correção funcional ainda.

## GATE 1.2 — TESTES DE REGRESSÃO ANTES DA CORREÇÃO

- **Comando ou inspeção:** `backend: npx jest src/modules/pts/pts.service.spec.ts --runInBand --no-cache`; `frontend: npx jest app/dashboard/pts/components/ChecklistSection.test.tsx app/dashboard/pts/components/PtsTableRow.test.tsx app/dashboard/pts/hooks/usePts.test.tsx --runInBand --no-cache`.
- **Evidência esperada:** os novos testes exigem invariantes ausentes no código-base: `deleted_at` nas seis contagens, `fieldset`/`legend` no grupo de checklist, `aria-label` na emissão de PDF e single-flight da confirmação de reprovação.
- **Classificação:** `TEST_BLOCKER` temporário se a reprodução não puder ser executada; não considerar a implementação verde antes de observar os testes falhando e depois passando.
- **Ação tomada:** adicionadas apenas regressões; nenhuma implementação funcional ainda.

### Saída observada

- `backend`: exit 1; `PtsService › getAnalyticsOverview` falhou porque a chamada recebida continha apenas `{ company_id: 'company-1' }`, sem `deleted_at`.
- `frontend`: exit 1; `usePts mutation safety` recebeu 2 chamadas de `reject` em vez de 1; `ChecklistSection` não encontrou o grupo acessível; `AtmosphericReadingsSection` não encontra campos com nome dos cabeçalhos; o teste do botão foi reforçado para exigir o atributo `aria-label` explícito.
- **Classificação:** `CONFIRMED` — as regressões foram reproduzidas antes da implementação.

## GATE 1.3 — REGRESSÃO PÓS-CORREÇÃO

- **Comando ou inspeção:** `backend: npx jest src/modules/pts/pts.service.spec.ts --runInBand --no-cache`; `frontend: npx jest app/dashboard/pts/components/ChecklistSection.test.tsx app/dashboard/pts/components/AtmosphericReadingsSection.test.tsx app/dashboard/pts/components/PtsTableRow.test.tsx app/dashboard/pts/hooks/usePts.test.tsx --runInBand --no-cache`.
- **Evidência:** backend exit 0, `1 passed, 57 passed, 57 total`; frontend exit 0, `4 passed, 6 passed`. As cinco regressões observadas no gate anterior passaram após as correções.
- **Classificação:** `CONFIRMED` — analytics agora exclui PTs removidas, o fluxo de rejeição tem barreira síncrona contra double-submit e os controles auditados possuem semântica/acessibilidade coberta por testes.
- **Ação tomada:** mantidas as menores alterações em `pts.service.ts`, `usePts.ts`, `PtsTableRow.tsx`, `ChecklistSection.tsx` e `AtmosphericReadingsSection.tsx`; nenhum arquivo APR ou backend fora do módulo PT foi alterado.

## GATE 1.4 — CONCORRÊNCIA DA LISTAGEM E STALE ASYNC DO FORMULÁRIO

- **Comando ou inspeção:** inspeção de `frontend/app/dashboard/pts/hooks/usePts.ts:77-180` e `frontend/app/dashboard/pts/components/PtForm.tsx:1273-1735`; `npx jest app/dashboard/pts/hooks/usePts.test.tsx --runInBand --no-cache` antes e depois do guard.
- **Evidência:** antes do guard, o teste `não permite que uma resposta de página antiga sobrescreva a página atual` falhou com esperado `pt-page-three` e recebido `pt-page-two`; após o guard, exit 0 com `1 passed, 2 passed`. O `PtForm` mantém `let active = true`, verifica `if (!active) return` após awaits e executa `active = false` no cleanup em `PtForm.tsx:1274-1535`, `1540-1596`, `1598-1658` e `1660-1735`.
- **Classificação:** `CONFIRMED` — corrida de paginação frontend corrigida com geração monotônica por request; `PASS` no stale async já existente do `PtForm`, coberto pela proteção de ciclo de vida e pelos testes da suíte do componente.
- **Ação tomada:** adicionada a barreira de geração em `usePts`; nenhum refactor de data layer e nenhum arquivo APR alterado.

## GATE 1.5 — ATOMICIDADE DE MUTAÇÕES DE STORAGE DO PT

- **Comando ou inspeção:** testes focused antes/depois em `backend/src/modules/pts/pts.service.spec.ts`; inspeção de `attachChecklistItemAttachment` e `removeEvidencePhoto`.
- **Evidência:** antes da correção, a gravação simuladamente falha e os testes observaram remoção prematura do objeto antigo no storage; saída focused: falha nos casos `não remove o anexo anterior se a gravação da substituição falhar` e `preserva a foto no storage se a gravação da remoção falhar`. Depois: `npx jest src/modules/pts/pts.service.spec.ts --runInBand --no-cache` exit 0, `1 passed, 60 passed, 60 total`. Em `backend/src/modules/pts/pts.service.ts:1778-1804`, a foto só é removida do storage após o commit; em `:1891-1934`, o anexo anterior só é limpo após a substituição persistir; em `:1917-1927`, o upload novo é compensado se a transação falhar. Regressões em `backend/src/modules/pts/pts.service.spec.ts:1869-1955`.
- **Classificação:** `CONFIRMED` — data-loss frontend/backend de storage corrigido dentro do módulo PT.
- **Ação tomada:** mover limpeza destrutiva para depois do commit e preservar compensação do upload novo; nenhuma migration, backend fora de PT ou produção alterados.

## GATE 1.6 — DELETE/WORKFLOW COM LOCK E LIMPEZA PÓS-COMMIT

- **Comando ou inspeção:** teste focused de `remove()` antes/depois; inspeção de `backend/src/modules/pts/pts.service.ts:1386-1432` e `:2160-2215`.
- **Evidência:** antes da correção, o teste exigindo transação/lock falhou (`1 failed, 59 passed, 60 total`); depois, `npx jest src/modules/pts/pts.service.spec.ts --runInBand --no-cache` exit 0, `1 passed, 60 passed, 60 total`. O helper executa `SELECT ... company_id ... deleted_at IS NULL FOR UPDATE NOWAIT` em `:1403-1415`, e `remove()` usa o mesmo manager de transação para governança/soft-delete em `:2183-2201`; a remoção física é posterior ao commit em `:2206-2215`. A atualização também usa o helper em `:1080` e `:1456-1618`.
- **Classificação:** `CONFIRMED` — corrida de workflow e janela de data-loss durante exclusão corrigidas no serviço PT.
- **Ação tomada:** consolidado lock pessimista com retry/conflict e limpeza de objeto após commit; não removida nenhuma validação de status, tenant ou governança.

## GATE 1.7 — SUBSTITUIÇÃO ATÔMICA DE ASSINATURAS DA PT

- **Comando ou inspeção:** inspeção do fluxo anterior em `frontend/app/dashboard/pts/components/PtForm.tsx:1175-1220`; implementação de `PUT /pts/:id/signatures`; `npx jest src/modules/pts/pts.service.spec.ts src/modules/pts/pts.controller.spec.ts --runInBand --no-cache`; `npm test -- --runTestsByPath src/services/ptsService.test.ts app/dashboard/pts/components/PtForm.test.tsx app/dashboard/pts/components/PtSignatureActions.test.tsx --runInBand --no-cache`.
- **Evidência:** o fluxo anterior removia assinaturas via `DELETE /signatures/:id` antes de criar substitutas em chamadas separadas. O novo contrato está em `backend/src/modules/pts/pts.controller.ts:149-172`, DTO em `backend/src/modules/pts/dto/replace-pt-signatures.dto.ts:1-38`, transação/lock/validação de executantes em `backend/src/modules/pts/pts.service.ts:2163-2251`, cliente em `frontend/src/services/ptsService.ts:514-523` e formulário em `frontend/app/dashboard/pts/components/PtForm.tsx:1175-1185`. Testes pós-correção: backend `2 suites passed, 69 tests passed`; frontend `3 suites passed, 49 tests passed`; full suites posteriores também passaram. A execução red do código antigo não foi repetida em checkout destrutivo; permanece `TEST_BLOCKER` de reprodução histórica, documentado honestamente, porque a regressão foi adicionada após a inspeção e a worktree concorrente não podia ser revertida.
- **Classificação:** `CONFIRMED` — janela de perda de assinatura corrigida no fluxo PT; `TEST_BLOCKER` somente para reproduzir o estado histórico sem desfazer a worktree.
- **Contrato backend esperado/observado/risco:** esperado: `PUT /pts/:id/signatures` receber `{signatures:[{user_id,signature_data,type,pin?}]}` e responder `{entityId,replaced}`, de forma atômica e limitada a executantes do PT. Observado: contrato implementado/testado localmente, mas não exercitado em backend live. Risco residual: divergência de deploy/DB ou falha de integração externa não pode ser provada nesta worktree.
- **Ação tomada:** criado endpoint PT com `can_manage_pt` + `can_manage_signatures`, lock do PT, validação de usuário executante e substituição transacional limitada ao escopo PT; removida a sequência frontend delete-then-create. Nenhum arquivo APR ou módulo compartilhado de assinaturas foi alterado.

## GATE 1.8 — LIMPEZA PÓS-COMMIT DA EVIDÊNCIA DE ASSINATURA PT

- **Comando ou inspeção:** revisão de `backend/src/modules/signatures/signatures.service.ts:280-348,1519-1540` e `backend/src/modules/pts/pts.service.ts`; `npx jest src/modules/pts/pts.service.spec.ts --runInBand --no-cache`; `$env:NODE_OPTIONS='--max-old-space-size=4096'; npm run type-check`.
- **Evidência:** o fluxo compartilhado limpava arquivos antigos logo após a operação do `EntityManager` recebido, o que seria prematuro quando chamado dentro da transação externa da PT. O fluxo PT agora registra as chaves antigas e executa `cleanupPtSignatureEvidenceFiles` somente depois do retorno bem-sucedido de `executePtWorkflowTransition` (`backend/src/modules/pts/pts.service.ts:2163-2268,2271-2299`); em falha da transação, compensa evidências recém-criadas sem remover as antigas. A regressão `só limpa a evidência antiga depois do commit da transação externa` em `backend/src/modules/pts/pts.service.spec.ts:835-924` passou junto com `63` testes focused; type-check exit `0`.
- **Classificação:** `CONFIRMED` — janela P0 de data-loss por limpeza de evidência antiga antes do commit externo corrigida exclusivamente no fluxo PT.
- **Ação tomada:** implementada limpeza pós-commit local ao módulo PT e compensação de objetos novos em falha; não alterado o serviço compartilhado, APR, migrations ou produção.

## GATE 1.9 — COMPENSAÇÃO DE UPLOAD PARCIAL DE ASSINATURAS PT

- **Comando ou inspeção:** revisão do loop de persistência em `backend/src/modules/pts/pts.service.ts:2223-2249`; `npx jest src/modules/pts/pts.service.spec.ts --runInBand --no-cache`; `npm run lint`; `npm run type-check`.
- **Evidência:** a lista de evidências recém-criadas agora é registrada antes do loop (`backend/src/modules/pts/pts.service.ts:2223-2225`), permitindo compensar a primeira assinatura quando uma assinatura posterior falha. A regressão `compensa evidência já criada quando uma assinatura posterior falha` (`backend/src/modules/pts/pts.service.spec.ts:945-1018`) passou; focused `1 suite, 64 tests`; lint e type-check exit `0`.
- **Classificação:** `CONFIRMED` — vazamento de objeto novo em falha parcial de substituição corrigido no fluxo PT.
- **Ação tomada:** menor mudança local para preservar cleanup compensatório; nenhum módulo compartilhado, APR, migration ou produção alterado.

## GATE 1.10 — CONTRATO PT PARA ASSINATURA AVULSA

- **Comando ou inspeção:** comparação de `frontend/app/dashboard/pts/components/PtSignatureActions.tsx:43-55` com o fluxo `PUT /pts/:id/signatures`; implementação de `POST /pts/:id/signatures`; `npx jest src/modules/pts/pts.service.spec.ts src/modules/pts/pts.controller.spec.ts --runInBand --no-cache`; `npm test -- --runTestsByPath app/dashboard/pts/components/PtSignatureActions.test.tsx app/dashboard/pts/components/PtsTableRow.test.tsx src/services/ptsService.test.ts --runInBand --no-cache`.
- **Evidência:** antes, a ação da tabela usava o endpoint genérico de assinaturas e não aplicava a regra PT de executante. Agora o cliente usa `frontend/src/services/ptsService.ts:525-535` e o endpoint `POST /pts/:id/signatures` em `backend/src/modules/pts/pts.controller.ts:180-198`, com DTO sem `user_id/company_id` em `backend/src/modules/pts/dto/create-pt-signature.dto.ts:1-23`. O serviço aplica lock, valida `pt_executantes`, rejeita assinatura ativa duplicada e delega a persistência no manager em `backend/src/modules/pts/pts.service.ts:2272-2345`. Regressões: backend `2 suites passed, 75 tests passed`; frontend `3 suites passed, 43 tests passed`; full suites posteriores passaram.
- **Classificação:** `CONFIRMED` — inconsistência P1 de autorização/integridade do fluxo de assinatura avulsa corrigida no escopo PT.
- **Contrato backend esperado/observado/risco:** esperado: `POST /pts/:id/signatures` receber `{signature_data,type,pin?}`, derivar o signatário do JWT, exigir vínculo em `pt_executantes`, rejeitar duplicata ativa e responder `{entityId,created}`. Observado: implementado e testado localmente; backend live não exercitado. Risco residual: divergência de deploy/RLS/storage remoto.
- **Ação tomada:** removido o uso do endpoint genérico pelo PT; nenhum módulo compartilhado de assinaturas, APR, migration ou produção alterado.

## GATE 2.1 — FRONTEND PT: MUTATION SAFETY, STALE ASYNC E ACESSIBILIDADE SEMÂNTICA

- **Comando ou inspeção:** regressões focused antes/depois e suíte do módulo: `npm test -- --runTestsByPath app/dashboard/pts/components/AtmosphericReadingsSection.test.tsx app/dashboard/pts/components/ChecklistSection.test.tsx`; `npm test -- --runTestsByPath` com os 11 testes de componentes/hooks PT.
- **Evidência:** antes da correção, o upload concorrente falhou com esperado `1` e recebido `2`; depois o focused terminou `2 suites passed, 5 tests passed`. A suíte PT anterior terminou `11 suites passed, 35 tests passed`; após a integração de assinaturas, `167` suítes frontend passaram no `test:ci`. `frontend/app/dashboard/pts/hooks/usePts.ts:84-95,156-180,259-274,749-874` usa geração monotônica para paginação e lock síncrono por mutação; `frontend/app/dashboard/pts/components/AtmosphericReadingsSection.tsx:69-112` e `ChecklistSection.tsx:57-118` bloqueiam duplo append/upload; `ChecklistSection.tsx:184-291` e `RapidRiskAnalysisSection.tsx:89-135` usam `fieldset`/`legend`, required e erro associado; `AtmosphericReadingsSection.tsx:134-186` usa cabeçalho/`aria-labelledby`; `PtsTableRow.tsx:232-244` nomeia o botão de PDF; `PtMobileCard.tsx:230-235` mantém edição desabilitada para PT não pendente; `PtClosureModal.tsx:83-175` usa `ModalFrame` e foco inicial; `PtEvidencePhotosSection.tsx:51-83,85-118,270-300` protege thumbnails, upload, confirmação e remoção; `PtSignatureActions.tsx:39-61` bloqueia assinatura avulsa concorrente; `PtForm.tsx:1175-1185` usa o replace transacional PT.
- **Classificação:** `CONFIRMED` — stale state severo, double-submit nos caminhos PT auditados e lacunas semânticas P1 corrigidos com regressões.
- **Ação tomada:** menor mudança local, sem trocar router/state manager/UI library; confirm nativo do fluxo PT substituído por `ConfirmModal`; URLs de artefato passam por allowlist e não há sink DOM XSS dinâmico encontrado nos arquivos PT.

## GATE 2.2 — ROTAS, CONTRATOS, TENANT/RBAC E BANCO ESTÁTICO

- **Comando ou inspeção:** leitura de `backend/src/modules/pts/pts.controller.ts`, `pts.service.ts`, DTOs/entidade, `frontend/src/services/ptsService.ts`, páginas PT, migrations RLS/índices e busca repo-local por matcher/tenant/status/storage.
- **Evidência:** controller PT aplica `JwtAuthGuard`, `TenantGuard`, `RolesGuard` e `TenantInterceptor`, com `Authorize` por capacidade nas rotas; ids usam `ParseUUIDPipe` e índices usam `ParseIntPipe`; service verifica empresa/site/status e consultas analíticas/exportações usam `deleted_at IS NULL` (`backend/src/modules/pts/pts.service.ts:2454-2503`). Entidade/migrations mantêm soft delete, unique parcial por empresa/número, integridade de status, FKs e RLS/FORCE RLS, incluindo junction `pt_executantes`; as políticas estão em `backend/src/infra/database/migrations/1709000000021-rls-all-tenant-tables.ts:98-126`, `backend/src/infra/database/migrations/1709000000029-rls-tenant-policy-with-check.ts:84-104` e `backend/src/infra/database/migrations/1709000000106-rls-junction-tables-and-apr-children.ts:102-130`. O frontend remove `company_id` arbitrário de bodies em `frontend/src/services/ptsService.ts`; o middleware anti-spoofing foi inspecionado em `backend/src/shared/middleware/tenant.middleware.ts:119-177,214-232`.
- **Classificação:** `PASS` estático para rotas/guards/contratos observáveis; `BACKEND_CONTRACT_BLOCKER` e `NÃO VERIFICADO` para RLS live, autorização real, exports/downloads/storage e isolamento sob banco executando.
- **Contrato backend esperado/observado/risco:** esperado: identidade/tenant/ownership/permissão/estado derivados e revalidados no servidor para cada leitura/mutação/arquivo/export; RLS deve estar habilitado e aplicável no banco real. Observado: código e migrations possuem controles, mas este gate não acessou banco ou ambiente remoto. Risco: uma policy aplicada incorretamente em runtime pode permitir cross-tenant apesar da evidência estática.
- **Ação tomada:** nenhum acesso remoto, migration, alteração de produção ou relaxamento de autorização.

## GATE 2.3 — REVOGAÇÃO DA BLOB URL DE IMPRESSÃO LOCAL PT

- **Comando ou inspeção:** busca por `URL.createObjectURL`/`URL.revokeObjectURL` nos caminhos PT; `npm test -- --runTestsByPath app/dashboard/pts/hooks/usePts.test.tsx --runInBand --no-cache`.
- **Evidência:** a impressão local de PT pendente criava uma URL temporária sem revogação em `frontend/app/dashboard/pts/hooks/usePts.ts:715-722`. Foi adicionada revogação agendada em `:721`; a regressão `revoga a URL temporária quando imprime uma cópia local` em `frontend/app/dashboard/pts/hooks/usePts.test.tsx:164-202` passou (`1` suíte, `3` testes).
- **Classificação:** `CONFIRMED` — vazamento de recurso P2 no caminho de impressão local corrigido.
- **Ação tomada:** adicionada somente a revogação tardia após abertura da impressão; nenhum novo pacote, backend ou contrato foi alterado.

## GATE 3 — BROWSER / RESPONSIVIDADE / FOCO / AUTHENTICATED QA

- **Comando ou inspeção:** build frontend com `NEXT_PUBLIC_API_URL=https://api.sgsseguranca.com.br` e `NEXT_PUBLIC_APP_URL=https://app.sgsseguranca.com.br`; servidor local `npm start -- -p 3000`; `npx --yes --package @playwright/cli playwright-cli --session ptqa open http://127.0.0.1:3000/dashboard/pts --headed`; snapshots, screenshot em 1040x752/768x1024/390x844 e `press Tab`.
- **Evidência:** build exit 0 e rotas `/dashboard/pts`, `/dashboard/pts/new`, `/dashboard/pts/edit/[id]` foram geradas. A navegação para `/dashboard/pts` redirecionou para `/login`; snapshots confirmaram campos CPF/senha, skip link e foco por teclado avançando CPF → senha → mostrar senha. Screenshots: `output/playwright/pt-login-desktop.png`, `pt-login-tablet.png`, `pt-login-mobile.png`; não foi observado overflow evidente no shell público. O console registrou `Connecting to 'https://app.sgsseguranca.com.br/proxy/auth/csrf?...' violates CSP connect-src 'self' https://api.sgsseguranca.com.br ...`; inspeção de `frontend/src/lib/api.ts:18-43`, `frontend/src/lib/public-api-url.ts:17-20` e `frontend/proxy.ts:37-92` confirmou mismatch de configuração local (API pública canônica normalizada para proxy da app, CSP local permite apenas a origem API e localhost).
- **Classificação:** `BLOCKED` para PT autenticado, formulário completo, foco interno de modais, teclado em todos os passos, reduced-motion e QA de dados reais: nenhuma credencial foi fornecida e não foi usado ambiente de produção. `PASS` limitado ao build/rotas compiladas e shell público responsivo/foco básico. O erro CSP é `ENVIRONMENT_CONFIGURATION_BLOCKER`, não achado funcional confirmado do PT.
- **Ação tomada:** servidor local encerrado de forma controlada; PID exato do listener 3000 finalizado e verificação retornou `NO_LISTENER_3000`. Nenhuma tentativa de login, segredo, requisição mutável ou alteração remota.

## GATE 3.1 — E2E PÚBLICO DISPONÍVEL

- **Comando ou inspeção:** `NEXT_PUBLIC_API_URL=http://127.0.0.1:3011 NEXT_PUBLIC_APP_URL=http://127.0.0.1:3100 npm run test:e2e -- e2e/public-mobile.spec.ts`; tentativa de preparação com `npx playwright install chromium webkit`.
- **Evidência:** a suíte foi descoberta em `frontend/e2e/public-mobile.spec.ts` e iniciou 24 combinações, mas todos os casos falharam antes do primeiro passo com `browserType.launch: Executable doesn't exist` para `chromium_headless_shell-1243`/`webkit-2359`. A instalação tentou `https://cdn.playwright.dev/builds/cft/153.0.8010.12/win64/chrome-win64.zip` e terminou exit 1 após timeout de 30s repetido: `Failed to install browsers`.
- **Classificação:** `EVIDENCE_BLOCKED` — bloqueio do cache de browsers/CDN do runner; não há resultado funcional da suíte E2E pública. O browser headed separado foi executado com sucesso contra o shell público local e está registrado no Gate 3.
- **Ação tomada:** não alterado `playwright.config.ts`, não baixado browser de origem alternativa, nenhum cenário autenticado foi fabricado e nenhuma requisição mutável remota foi feita.

## GATE FINAL — VALIDAÇÃO REPRODUZÍVEL

- **Comando ou inspeção:** backend: `npm run lint`, `npm run type-check`, `npm run test:ci`, `npm run build`, `npm audit --audit-level=high`; frontend: `npm run lint`, `npx tsc --noEmit --pretty false`, `npm run test:ci`, build com as duas variáveis públicas explícitas, `npm audit --audit-level=high`; `git diff --check`.
- **Evidência:** backend lint/typecheck/build/audit exit 0; `test:ci` exit 0, `325 passed` suítes e `2850 passed` testes. Frontend lint/typecheck/build/audit exit 0; `test:ci` exit 0, `167 passed` suítes de `168` (1 skipped), `908 passed` de `910` (2 skipped); build Next 16.3.4 compilou, executou TypeScript, gerou 91/91 páginas e listou as três rotas PT. Ambos os audits retornaram `found 0 vulnerabilities`; `git diff --check` sem saída funcional (somente warnings EOL preexistentes em `verify`).
- **Classificação:** `PASS` nos checks executáveis; `STATUS=PARTIAL` pela falta de QA autenticado/browser completo, banco/RLS live e contratos backend/storage exercitados.
- **Ação tomada:** revisão do diff e do escopo mantida em PT + docs de auditoria; arquivos `verify` seguem EOL-only preexistentes e não foram tocados; nenhum arquivo APR foi alterado. `frontend/CLAUDE.md` apareceu modificado pelo `next dev` durante o QA, não foi editado por esta auditoria e foi preservado como mudança concorrente fora do escopo.

## GATE FINAL-RECHECK — APÓS ATOMICIDADE, LIMPEZA E COMPENSAÇÃO DE ASSINATURAS

- **Comando ou inspeção:** `npx jest src/modules/pts/pts.service.spec.ts src/modules/pts/pts.controller.spec.ts --runInBand --no-cache`; `npm test -- --runTestsByPath src/services/ptsService.test.ts app/dashboard/pts/components/PtForm.test.tsx app/dashboard/pts/components/PtSignatureActions.test.tsx --runInBand --no-cache`; backend/frontend `test:ci`, lint, typecheck, build e audit; `git fetch origin`; `git diff --check`; verificação de listeners 3000/3100.
- **Evidência:** focused backend `2 suites passed, 75 tests passed`; full backend `325 suites passed, 2850 tests passed`; focused frontend `3 suites passed, 43 tests passed`; full frontend `167 suites passed, 908 tests passed`, com os skips já registrados. Lint, typecheck, build e audit permaneceram exit 0; ambos audits retornaram `found 0 vulnerabilities`; `origin/main` continuou em `5a5ae66ae8eb6410ade8a4b0e68dbd3105fc6219`; a checagem final retornou `NO_LISTENER_3000_3100`. `git diff --check` não encontrou erro, somente avisos EOL dos arquivos `verify` excluídos.
- **Classificação:** `PASS` automatizado e estático do código PT; `STATUS=PARTIAL` global por ausência de RLS/backend live e QA autenticado/browser completo.
- **Ação tomada:** nenhum commit, push, PR, merge ou deploy; nenhuma alteração em APR, migrations ou produção.

## GATE FINAL-SCOPE-RECHECK — FRONTEIRA E ARTEFATOS PREEXISTENTES

- **Comando ou inspeção:** `git status --short --branch`; enumeração de caminhos modificados/untracked; contagem por escopo; `git diff --ignore-space-at-eol -- frontend/app/verify/page.tsx frontend/app/verify/page.test.tsx`; `git rev-parse HEAD`; `git rev-parse origin/main`.
- **Evidência:** `APR=0`, `MIGRATIONS=0`, `WORKFLOWS=0`; alterações funcionais limitadas aos arquivos PT/backend do módulo, testes correspondentes e `docs/audits/pt-module-hardening-*`. HEAD e `origin/main` permanecem `5a5ae66ae8eb6410ade8a4b0e68dbd3105fc6219`. `VERIFY_EOF_DIFF=ZERO_LINES`; os únicos avisos do `git diff --check` são os CRLF históricos dos dois arquivos `verify`. `frontend/CLAUDE.md` continua preservado como mudança concorrente gerada fora desta auditoria.
- **Classificação:** `PASS` de fronteira da worktree; `STATUS=PARTIAL` global permanece por evidência live/autenticada ausente.
- **Ação tomada:** nenhuma limpeza destrutiva, normalização EOL, alteração em APR/migrations/workflows ou publicação; somente este registro documental foi atualizado.

## GATE 2.4 — LIMITE DE ENTRADA DO ARRAY DE EXECUTANTES

- **Comando ou inspeção:** criação da regressão em `backend/src/modules/pts/dto/create-pt.dto.spec.ts`; execução pré-correção com `npx jest src/modules/pts/dto/create-pt.dto.spec.ts --runInBand --no-cache`; aplicação de `@ArrayMaxSize(200)` em `CreatePtDto`; repetição do mesmo comando.
- **Evidência:** antes da correção, o teste de 201 executantes falhou com `Expected: > 0 / Received: 0`; depois, a execução terminou `1 passed` suíte e `2 passed` testes. O limite está em `backend/src/modules/pts/dto/create-pt.dto.ts:162-166`, e cobre também `UpdatePtDto` por `PartialType`.
- **Classificação:** `CONFIRMED` — lacuna P2 de consumo de recurso/validação de payload no contrato de escrita do PT corrigida; o limite não altera payloads normais e impede uma lista arbitrariamente grande antes da consulta `IN` de usuários.
- **Ação tomada:** adicionado limite explícito de 200 IDs, alinhado ao limite operacional já usado no módulo APR; nenhuma mudança de banco, migration, APR ou frontend foi necessária.

## GATE 2.5 — RLS MULTI-SITE DO PT

- **Comando ou inspeção:** leitura de `backend/src/infra/database/migrations/1709000000127-harden-site-scoped-tenant-rls.ts`, `backend/src/infra/database/migrations/1709000000367-enable-multi-site-rls-scope.ts`, `backend/src/shared/database/tenant-db-context.service.ts` e `backend/src/shared/tenant/site-access-scope.util.ts`; busca `rg -n "site_id = ANY\\(current_site_ids\\(\\)\\)|current_site_ids|EnableMultiSiteRlsScope" backend/src`.
- **Evidência:** a policy histórica 0127 usava apenas `site_id = current_site_id()` (`.../1709000000127-harden-site-scoped-tenant-rls.ts:75-93`), mas a migration posterior 0367 reescreve as policies das tabelas com `company_id`/`site_id`, inclusive `pts`, usando `site_id = ANY(current_site_ids())` (`.../1709000000367-enable-multi-site-rls-scope.ts:54-75,121-169`). A mesma migration trata `users`/`sites`; o contexto configura `app.current_site_ids`. A busca confirmou os símbolos e a regra de lista multi-site.
- **Classificação:** `PASS` estático para a divergência investigada; `NÃO VERIFICADO/BACKEND_CONTRACT_BLOCKER` para a aplicação efetiva da cadeia de migrations e o comportamento RLS em banco live. Não foi criado patch novo porque a correção já existe na base auditada e abrange o PT.
- **Contrato backend esperado/observado/risco:** esperado: usuário com vários `siteIds` enxerga apenas esses sites, e usuário sem escopo válido falha fechado; observado: código/migration 0367 implementam o predicado multi-site, mas não houve conexão ao banco de teste nesta etapa. Risco residual: migration pendente, drift ou contexto live mal configurado pode negar disponibilidade ou quebrar isolamento; requer `verify:rls`/teste autenticado no banco de QA.
- **Ação tomada:** nenhuma alteração em migration; registrado explicitamente para não reabrir a migration histórica 0127 nem tocar APR. `MIGRATIONS_CHANGED=0` permanece verdadeiro.

## GATE FINAL-RECHECK-2 — APÓS LIMITE DE DTO E REVISÃO RLS

- **Comando ou inspeção:** backend: `npm run lint; npm run type-check; npm run test:ci; npm run build; npm audit --audit-level=high`; `npx jest src/modules/pts/pts-migrations.spec.ts --runInBand --no-cache`; frontend: `npm run test:ci`; `npm run lint; npx tsc --noEmit --pretty false;` build com `NEXT_PUBLIC_API_URL` e `NEXT_PUBLIC_APP_URL` públicas explícitas; `npm audit --audit-level=high`.
- **Evidência:** backend terminou exit `0`: lint/type-check/build/audit verdes, `326` suítes e `2852` testes passados; migration focused `1` suíte e `20` testes passados; audit retornou `found 0 vulnerabilities`. Frontend terminou exit `0`: `167` suítes passadas de `168` (1 skipped), `908` testes passados de `910` (2 skipped), lint/typecheck/build/audit verdes; Next compilou e gerou `91/91` páginas, incluindo as três rotas PT; audit retornou `found 0 vulnerabilities`.
- **Classificação:** `PASS` local automatizado; `STATUS=PARTIAL` global permanece por RLS/backend/storage live, QA autenticado, E2E com browser instalado, foco interno completo, reduced-motion e validação de dados reais não exercitados.
- **Ação tomada:** nenhuma execução remota, migration, alteração em APR/CI/produção ou publicação. O único patch funcional adicional desta rodada foi `@ArrayMaxSize(200)` no DTO PT e seu teste de regressão.

## GATE 2.6 — CONTEXTO DE APR STALE NO PTFORM

- **Comando ou inspeção:** criação do teste deferred `ignora o contexto de APR que chega depois de uma seleção mais recente`; execução focused de `PtForm.test.tsx` sem o guard de geração; restauração do guard e repetição com `--testNamePattern="(ignora o contexto de APR|mantém a resposta mais recente)"`.
- **Evidência:** sem o guard, o teste reproduziu falha ao aplicar a APR antiga depois da nova (`Unable to find ... Altura atual: false`), demonstrando que o contexto tardio reintroduzia o risco de altura. Com a correção, `Test Suites: 1 passed` e `Tests: 2 passed` no comando focused. A proteção está em `frontend/app/dashboard/pts/components/PtForm.tsx:497,1025-1038,1088`; o teste está em `frontend/app/dashboard/pts/components/PtForm.test.tsx:736-779`.
- **Classificação:** `CONFIRMED` — stale async P1 no contexto de risco da APR, corrigido com geração monotônica para ignorar resposta, erro e efeitos tardios de seleção anterior.
- **Ação tomada:** menor mudança local com `useRef`; nenhum endpoint, contrato backend, APR ou camada de dados alterado.

## GATE 2.7 — REVALIDAÇÃO STALE DA PT APÓS MUTAÇÕES DE EVIDÊNCIA

- **Comando ou inspeção:** criação do teste deferred `mantém a resposta mais recente ao revalidar a PT após mutações concorrentes`; execução focused após remover temporariamente somente `currentPtRefreshGenerationRef`/comparação; restauração e repetição do teste focused e da suíte completa de `PtForm.test.tsx`.
- **Evidência:** sem a barreira, a resposta antiga sobrescreveu a mais nova e o teste falhou em `Unable to find ... Fotos atuais: 1`. Com a barreira, o focused terminou `Test Suites: 1 passed; Tests: 2 passed`, e a suíte completa terminou `Test Suites: 1 passed; Tests: 12 passed`. A proteção está em `frontend/app/dashboard/pts/components/PtForm.tsx:498,631-640`; o teste está em `frontend/app/dashboard/pts/components/PtForm.test.tsx:782-829`.
- **Classificação:** `CONFIRMED` — race P1/P2 de estado stale após mutações assíncronas do PT, corrigido; a resposta da geração mais antiga não pode remover evidência que chegou em uma resposta mais recente.
- **Ação tomada:** guard de geração por `useRef` em `refetchCurrentPt`; nenhuma chamada remota, alteração de storage ou mudança de contrato backend.

## GATE FINAL-RECHECK-3 — APÓS OS GUARDS STALE DO PTFORM

- **Comando ou inspeção:** `npm run test:ci`; `npm run lint`; `npx tsc --noEmit --pretty false`; build com `NEXT_PUBLIC_API_URL=https://api.sgsseguranca.com.br` e `NEXT_PUBLIC_APP_URL=https://app.sgsseguranca.com.br`; `npm audit --audit-level=high`; revisão de `git diff --check` e fronteira de caminhos.
- **Evidência:** frontend `test:ci` exit `0`, `1` suíte skipped e `167` passed de `168`; `2` testes skipped e `910` passed de `912`. Lint, typecheck, build e audit exit `0`; Next compilou, executou TypeScript, gerou `91/91` páginas e listou `/dashboard/pts`, `/dashboard/pts/new` e `/dashboard/pts/edit/[id]`; audit retornou `found 0 vulnerabilities`.
- **Classificação:** `PASS` para os checks locais executáveis; `STATUS=PARTIAL` global permanece por autenticação real, browser E2E completo, RLS/storage/contratos backend live, foco interno completo e reduced-motion não exercitados.
- **Ação tomada:** somente código/testes/documentação do PT foram mantidos nesta rodada; nenhum arquivo APR, migration, workflow, produção ou ambiente remoto foi alterado.

## GATE 3.2 — E2E PÚBLICO COM CHROME DO SISTEMA

- **Comando ou inspeção:** servidor local com `NEXT_PUBLIC_API_URL=https://api.sgsseguranca.com.br`, `NEXT_PUBLIC_APP_URL=http://127.0.0.1:3100`, `npm run dev -- --hostname 127.0.0.1 --port 3100`; `npx playwright test e2e/public-mobile.spec.ts` usando configuração transitória removida ao final para selecionar o Chrome instalado em `C:\Program Files\Google\Chrome\Application\chrome.exe`; execução final com `--workers=1 --reporter=line`.
- **Evidência:** `24 passed (50.3s)`. Foram exercitados login público sem overflow/zoom, recuperação de senha e redirecionamento sem sessão nos viewports 320x568, 360x800, 390x844, 430x932, 1440x900, 412x915, 768x1024 e 844x390. O servidor foi encerrado e não restou listener em 3100/3000; somente conexões `TIME_WAIT` foram observadas.
- **Classificação:** `PASS` para o E2E público e responsividade do shell não autenticado; `EVIDENCE_BLOCKED` permanece somente para E2E autenticado, passos internos do PT, foco interno/reduced-motion e mutações com dados reais.
- **Ação tomada:** nenhuma credencial, chamada mutável remota ou produção foi utilizada; a configuração transitória não foi mantida no repositório.

## GATE 2.8 — SELEÇÃO DE OBRA EM ESCOPO MULTI-SITE

- **Comando ou inspeção:** criação da regressão em `backend/src/modules/pts/pts.service.spec.ts:708-733`; execução pré-correção com `npm test -- --runTestsByPath src/modules/pts/pts.service.spec.ts --runInBand --no-cache --testNamePattern="preserva a obra permitida selecionada"`; correção em `backend/src/modules/pts/pts.service.ts:734-755`; repetição do mesmo comando.
- **Evidência:** antes da correção, o teste falhou com `Expected site_id: site-b` e recebeu `site_id: site-a`, embora `site-b` estivesse na allow-list `siteIds: ['site-a','site-b']`. Depois, `Test Suites: 1 passed` e `Tests: 1 passed`. O `create()` agora valida a allow-list e preserva `createPtDto.site_id`, evitando atribuição silenciosa à primeira obra do contexto.
- **Classificação:** `CONFIRMED` — bug de integridade funcional P1/P2 em escopo multi-site corrigido; não era cross-tenant porque ambas as obras estavam autorizadas, mas podia gravar a PT na obra errada e validar usuários contra a obra errada.
- **Ação tomada:** menor correção local no create de PT e teste de regressão; nenhum RLS/migration, APR, endpoint externo ou ambiente remoto alterado.

## GATE FINAL-RECHECK-4 — APÓS CORREÇÃO DE SELEÇÃO MULTI-SITE

- **Comando ou inspeção:** focused red/green do teste `preserva a obra permitida selecionada`; `npm test -- --runTestsByPath src/modules/pts/pts.service.spec.ts --runInBand --no-cache`; `npm run lint`; `npm run type-check`; `npm run build`; `npm audit --audit-level=high`.
- **Evidência:** red reproduzido antes da correção com `Expected site_id: site-b` e recebido `site-a`; focused verde depois com `1 suíte, 1 teste`. No estado final: `326` suítes e `2853` testes passados; lint exit `0`; typecheck exit `0`; build exit `0`; audit `found 0 vulnerabilities`. A correção está em `backend/src/modules/pts/pts.service.ts:734-755` e a regressão em `backend/src/modules/pts/pts.service.spec.ts:708-741`.
- **Classificação:** `PASS` nos checks automatizados locais do backend PT; `STATUS=PARTIAL` global permanece por RLS/contratos/storage live, QA autenticado interno e validação visual/foco/reduced-motion não executados.
- **Ação tomada:** nenhuma alteração em APR, migrations, workflows, produção, ambiente remoto, commit, push, PR, merge ou deploy.

## GATE FINAL-SCOPE-RECHECK-2 — SINKS PT E FRONTEIRA FINAL

- **Comando ou inspeção:** `rg` nos caminhos `frontend/app/dashboard/pts`, `frontend/src/services/ptsService.ts` e `backend/src/modules/pts` para `window.confirm`, `window.alert`, sinks DOM/XSS, `URL.createObjectURL/revokeObjectURL` e mutações; `git diff --check`; contagem de caminhos APR/migrations/workflows; `git diff --ignore-space-at-eol -- frontend/app/verify/page.tsx frontend/app/verify/page.test.tsx`.
- **Evidência:** nenhum `confirm/alert`, `dangerouslySetInnerHTML`, `innerHTML`, `DOMParser`, `eval` ou `new Function` no runtime PT; todas as `createObjectURL` identificadas no PT têm `revokeObjectURL` agendado ou passam pelo utilitário central (`frontend/app/dashboard/pts/hooks/usePts.ts:610-721`, `frontend/src/lib/print-utils.ts:3-10,21-53`); attachments/thumbnails usam allowlist (`frontend/app/dashboard/pts/components/ChecklistSection.tsx:124-134`, `PtEvidencePhotosSection.tsx:64-72`). `git diff --check` só emite os avisos EOL preexistentes de `verify`; `APR_FILES=0`, `MIGRATION_FILES=0`, `WORKFLOW_FILES=0`; o diff ignorando espaço nos dois arquivos `verify` permanece vazio.
- **Classificação:** `PASS` estático para os sinks e fronteira do módulo; `STATUS=PARTIAL` global permanece por evidência autenticada/live não disponível.
- **Ação tomada:** nenhum arquivo fora do PT/testes/documentação foi alterado; `frontend/CLAUDE.md` e os dois arquivos `verify` foram preservados como mudanças preexistentes/concorrentes; sem commit, push, PR, merge ou deploy.

## GATE 2.9 — EMISSÃO DE PDF FINAL NO PTFORM

- **Comando ou inspeção:** criação do teste síncrono `bloqueia duplo clique síncrono na emissão do PDF final`; execução focused sem o lock; restauração do lock em `frontend/app/dashboard/pts/components/PtForm.tsx:498,1866-1892`; repetição focused e suíte completa de `PtForm.test.tsx`.
- **Evidência:** sem o guard, o teste falhou com `Expected number of calls: 1 / Received number of calls: 2` em `attachFile`; com `pdfEmissionInFlightRef`, o focused passou (`1 suíte, 1 teste`) e a suíte completa passou (`1 suíte, 13 testes`).
- **Classificação:** `CONFIRMED` — double-submit P1 no fluxo destrutivo de emissão/registro do PDF final corrigido no frontend PT.
- **Ação tomada:** adicionada barreira síncrona por `useRef`, mantendo o `loading` visual existente; nenhuma alteração de contrato, backend, APR ou produção.

## GATE FINAL-RECHECK-5 — APÓS LOCK DE PDF FINAL

- **Comando ou inspeção:** frontend `npm run test:ci`; `npm run lint`; `npx tsc --noEmit --pretty false`; build com `NEXT_PUBLIC_API_URL` e `NEXT_PUBLIC_APP_URL` públicas explícitas; `npm audit --audit-level=high`.
- **Evidência:** `test:ci` exit `0`, `167` suítes passadas de `168` (1 skipped), `911` testes passados de `913` (2 skipped); lint/typecheck/build exit `0`; build Next 16.3.4 gerou `91/91` páginas e listou as três rotas PT; audit `found 0 vulnerabilities`.
- **Classificação:** `PASS` nos checks frontend locais executáveis; `STATUS=PARTIAL` global permanece por QA autenticado, banco/RLS/storage live, foco interno completo e reduced-motion.
- **Ação tomada:** nenhuma chamada remota mutável, credencial ou produção; configuração pública usada somente para build local; nenhuma alteração em APR, migrations, workflows ou publicação.

## GATE 2.10 — DUPLICIDADE DE EXECUTANTES NO CONTRATO DE ESCRITA

- **Comando ou inspeção:** criação da regressão em `backend/src/modules/pts/dto/create-pt.dto.spec.ts`; execução pré-correção com `npm test -- --runTestsByPath src/modules/pts/dto/create-pt.dto.spec.ts --runInBand --no-cache --testNamePattern="rejeita executantes duplicados"`; aplicação de `@ArrayUnique()` em `backend/src/modules/pts/dto/create-pt.dto.ts:16-18,160-166`; repetição focused, lint, typecheck, suíte completa, build e audit.
- **Evidência:** antes, a regressão falhou com `Expected: > 0 / Received: 0`; depois, a suíte DTO passou `3/3`. No estado final: backend `326/326` suítes e `2854/2854` testes, lint/typecheck/build exit `0`, audit `found 0 vulnerabilities`. A validação ocorre antes da persistência da relação `pt_executantes`; `UpdatePtDto` também herda a regra via `PartialType`.
- **Classificação:** `CONFIRMED` — integridade P1/P2 do contrato de executantes corrigida; IDs repetidos não chegam à junção nem produzem estado ambíguo.
- **Ação tomada:** menor alteração no DTO PT e teste de regressão; nenhum schema/migration, APR, frontend remoto ou produção alterado.

## GATE 2.11 — PRESERVAÇÃO FORENSE NA SUBSTITUIÇÃO DE ASSINATURAS

- **Comando ou inspeção:** ajuste do teste `substitui assinaturas da PT dentro da transação e limita aos executantes` em `backend/src/modules/pts/pts.service.spec.ts:864-871` para exigir `softDelete` e rejeitar `delete`; execução red com `npm test -- --runTestsByPath src/modules/pts/pts.service.spec.ts --runInBand --no-cache --testNamePattern="substitui assinaturas"`; correção em `backend/src/modules/pts/pts.service.ts:2211-2226`; repetição focused, `npm run test:ci`, `npm run lint`, `npm run type-check`, `npm run build` e `npm audit --audit-level=high`.
- **Evidência:** antes da correção, o teste falhou porque `softDelete` recebeu `0` chamadas; depois, focused `1 suíte passada`, `1 teste passado` (`66` testes ignorados). O fluxo agora seleciona apenas assinaturas ativas com `deleted_at: IsNull()` e usa `signatureRepository.softDelete(...)`; `Signature` mantém `@DeleteDateColumn` em `backend/src/modules/signatures/entities/signature.entity.ts:81-85`. Validação completa posterior: `326/326` suítes e `2854/2854` testes, lint/type-check/build exit `0`, audit `found 0 vulnerabilities`.
- **Classificação:** `CONFIRMED` — perda de histórico forense por hard-delete no fluxo PT corrigida; registros substituídos permanecem disponíveis para auditoria, enquanto a evidência física antiga continua sendo limpa somente após o commit.
- **Ação tomada:** menor mudança no fluxo PT; nenhuma alteração no serviço compartilhado de assinaturas, schema/migration, APR, produção ou ambiente remoto.

## GATE 2.12 — ESCOPO DE OBRA DO VIGIA DA PT

- **Comando ou inspeção:** criação da regressão `bloqueia vigia de outra obra ao criar PT` em `backend/src/modules/pts/pts.service.spec.ts:1231-1264`; execução pré-correção com `npm test -- --runTestsByPath src/modules/pts/pts.service.spec.ts --runInBand --no-cache --testNamePattern="bloqueia vigia de outra obra"`; correção em `backend/src/modules/pts/pts.service.ts:631-641`; repetição focused, `npm run test:ci`, `npm run lint`, `npm run type-check`, `npm run build` e `npm audit --audit-level=high`.
- **Evidência:** antes, a regressão falhou porque a PT era criada com `vigia_user_id` de outra obra; depois, focused `1 suíte passada`, `1 teste passado` (`67` testes ignorados). O vigia agora é incluído na lista validada por `assertUsersScopedToSite`; a suíte completa posterior passou `326/326` e `2855/2855`, lint/type-check/build exit `0` e audit `found 0 vulnerabilities`.
- **Classificação:** `CONFIRMED` — vínculo cross-site dentro do mesmo tenant corrigido no contrato de criação da PT; usuários company-scoped continuam permitidos conforme regra existente.
- **Ação tomada:** menor alteração em `validateRelatedEntityScope`; nenhum schema/migration, APR, frontend remoto ou produção alterado.

## GATE 2.13 — HIDRATAÇÃO DE EXECUTANTES NO LOCK DE APROVAÇÃO

- **Comando ou inspeção:** criação da regressão `carrega executantes dentro do lock antes de aplicar regras de aprovação` em `backend/src/modules/pts/pts.service.spec.ts:1448-1510`; execução pré-correção com `npm test -- --runTestsByPath src/modules/pts/pts.service.spec.ts --runInBand --no-cache --testNamePattern="carrega executantes dentro do lock"`; correção em `backend/src/modules/pts/pts.service.ts:1414-1436`; repetição focused, `npm run test:ci`, `npm run lint`, `npm run type-check`, `npm run build` e `npm audit --audit-level=high`.
- **Evidência:** antes, a aprovação falhou com `PT bloqueada pelas regras de segurança da empresa` porque o `SELECT * FROM pts ... FOR UPDATE` não traz a relação `pt_executantes`; depois, focused `1 suíte passada`, `1 teste passado` (`68` testes ignorados). O helper agora consulta `pt_executantes` dentro da mesma transação/lock e hidrata IDs antes de `assertCanApprove`. Suíte completa posterior: `326/326` e `2856/2856`, lint/type-check/build exit `0`, audit `found 0 vulnerabilities`.
- **Classificação:** `CONFIRMED` — aprovação podia ignorar executantes/assinaturas ou bloquear uma PT válida por relação ausente no carregamento transacional; corrigido sem remover nenhuma regra de segurança.
- **Ação tomada:** menor correção no carregamento do helper compartilhado do PT; ajustados somente mocks de teste para refletirem as relações reais. Nenhum schema/migration, APR, frontend remoto ou produção alterado.

## GATE 2.14 — ESCOPO DE OBRA EM CONTAGENS DO PT

- **Comando ou inspeção:** criação da regressão `aplica escopo de obra ao count para tenants multi-site` em `backend/src/modules/pts/pts.service.spec.ts:1434-1454`; execução pré-correção com `npm test -- --runTestsByPath src/modules/pts/pts.service.spec.ts --runInBand --no-cache --testNamePattern="aplica escopo de obra ao count"`; correção em `backend/src/modules/pts/pts.service.ts:2460-2483`; repetição focused, `npm run test:ci`, `npm run lint`, `npm run type-check`, `npm run build` e `npm audit --audit-level=high`.
- **Evidência:** antes da correção, o teste falhou porque `PtsService.count()` enviou somente `company_id`/`deleted_at`, sem `site_id`. Depois, o focused terminou `1 suíte passada`, `1 teste passado` (`69` testes ignorados). O método agora impõe `site_id = In(siteIds)` para escopo restrito, sem aceitar um filtro de obra mais amplo fornecido pelo chamador. Validação completa posterior: backend `326/326` suítes e `2857/2857` testes, lint/type-check/build exit `0`, audit `found 0 vulnerabilities`.
- **Classificação:** `CONFIRMED` — vazamento funcional P1 de agregados/alertas entre obras do mesmo tenant corrigido; a contagem usada por notificações agora respeita o escopo multi-site.
- **Ação tomada:** menor correção no serviço PT e regressão de contrato; nenhuma migration, schema, APR, frontend remoto, produção ou ambiente compartilhado alterado.

## GATE FINAL-CLOSURE-20260911 — REVALIDAÇÃO DE FRONTEIRA E SINKS

- **Comando ou inspeção:** `git status --porcelain` com contagem de caminhos APR/migrations/workflows; `git diff --ignore-space-at-eol -- frontend/app/verify/page.tsx frontend/app/verify/page.test.tsx`; `rg -n 'window\.confirm|window\.alert|dangerouslySetInnerHTML|innerHTML|DOMParser|\beval\(|new Function' frontend/app/dashboard/pts frontend/src/services/ptsService.ts backend/src/modules/pts`; `rg -n 'URL\.createObjectURL|URL\.revokeObjectURL|revokeBlobUrlLater'` nos caminhos PT e utilitário de impressão.
- **Evidência:** reexecutado após os Gates 2.11, 2.12 e 2.13: saída `STATUS_PATHS=34`, `APR_FILES=0`, `MIGRATION_FILES=0`, `WORKFLOW_FILES=0`, `VERIFY_NON_EOL_DIFF=0` (com os avisos EOL preexistentes de `verify`); `PT_RUNTIME_DANGEROUS_SINKS=NONE`. As blob URLs do PT estão em `frontend/app/dashboard/pts/hooks/usePts.ts:610-721` e todas têm `URL.revokeObjectURL` agendado; o utilitário compartilhado está em `frontend/src/lib/print-utils.ts:5-49`.
- **Classificação:** `PASS` para a fronteira estática atual e os sinks auditados; `STATUS=PARTIAL` global permanece porque autenticação interna, RLS/storage live e validação visual interna ainda dependem de ambiente/credenciais não disponíveis.
- **Ação tomada:** nenhuma alteração funcional nesta revalidação; preservadas `frontend/CLAUDE.md` e `frontend/app/verify/*` como mudanças preexistentes/concorrentes, sem commit, push, PR, merge ou deploy.

## GATE FINAL-RECHECK-6 — APÓS ESCOPO DE OBRA EM CONTAGENS

- **Comando ou inspeção:** focused `npm test -- --runTestsByPath src/modules/pts/pts.service.spec.ts --runInBand --no-cache --testNamePattern="aplica escopo de obra ao count"`; suíte `src/modules/pts/pts.service.spec.ts`; backend `npm run test:ci`, `npm run lint`, `npm run type-check`, `npm run build`, `npm audit --audit-level=high`; rechecagem de fronteira com `git status --porcelain`, contagem APR/migrations/workflows, `git diff --ignore-space-at-eol -- frontend/app/verify/page.tsx frontend/app/verify/page.test.tsx` e `git diff --check`.
- **Evidência:** focused `1 suíte passada, 1 teste passado`; suíte PT `70/70`; backend `326/326` suítes e `2857/2857` testes passados, lint/type-check/build exit `0`, audit `found 0 vulnerabilities`. Fronteira: `STATUS_PATHS=34`, `APR_FILES=0`, `MIGRATION_FILES=0`, `WORKFLOW_FILES=0`, `VERIFY_NON_EOL_DIFF=0`; `git diff --check` exit `0`, emitindo apenas os avisos históricos de CRLF dos arquivos `verify`.
- **Classificação:** `PASS` para a correção local e os checks automatizados; `STATUS=PARTIAL` global permanece por ausência de QA autenticado, RLS/storage/backend live e validação visual interna completa.
- **Ação tomada:** preservada a fronteira PT/testes/documentação; nenhum arquivo APR, migration, workflow, produção ou branch compartilhado foi alterado. Sem commit, push, PR, merge ou deploy.

## GATE 2.15 — LOCK DO PDF FINAL ANTES DO UPLOAD

- **Comando ou inspeção:** teste de regressão `adquire o lock da PT antes de enviar o PDF ao storage` em `backend/src/modules/pts/pts.service.spec.ts:407-463`; execução focused com `npm test -- --runTestsByPath src/modules/pts/pts.service.spec.ts --runInBand --no-cache --testNamePattern="PDF"`.
- **Evidência:** a reprodução anterior à correção falhou porque `registerFinalDocument` não recebia `transactionManager`; depois, o focused terminou `1 suíte passada`, `12 testes passados` e `60 ignorados`. `attachPdf` agora executa `executePtWorkflowTransition` e só chama `uploadFileWithCapability` depois do `SELECT ... FOR UPDATE NOWAIT` em `backend/src/modules/pts/pts.service.ts:1211-1270`; o registro de governança recebe o mesmo manager em `:1270`.
- **Classificação:** `CONFIRMED` — corrida P1 de integridade/double-submit no PDF final corrigida. Duas requisições concorrentes não passam pela validação e pelo upload sem serialização da PT; falhas após upload continuam com compensação.
- **Ação tomada:** movido o upload para dentro da transação sob lock, revalidada a obra/status do registro bloqueado e mantida a limpeza compensatória; nenhum contrato externo, migration, APR ou ambiente remoto foi alterado.

## GATE 2.16 — DUPLICIDADE NO REPLACEMENT DE ASSINATURAS

- **Comando ou inspeção:** criação da regressão em `backend/src/modules/pts/dto/replace-pt-signatures.dto.spec.ts`; execução pré-correção com `npm test -- --runTestsByPath src/modules/pts/dto/replace-pt-signatures.dto.spec.ts --runInBand --no-cache`; correção em `backend/src/modules/pts/dto/replace-pt-signatures.dto.ts:4,38-41`; repetição do comando.
- **Evidência:** antes da correção, o DTO aceitou duas entradas com o mesmo `user_id` (`Received: undefined` para o erro de `signatures`); depois, `1 suíte passada`, `1 teste passado`. `@ArrayUnique((signature) => signature.user_id)` impede estado ambíguo antes do replacement transacional.
- **Classificação:** `CONFIRMED` — integridade P1/P2 do contrato de assinaturas corrigida; um mesmo usuário não pode ocupar duas posições na mesma substituição.
- **Ação tomada:** adicionada apenas validação declarativa no DTO, preservando o limite de array e o restante do contrato; nenhum schema/migration, APR, produção ou storage remoto foi alterado.

## GATE 2.17 — ATUALIZAÇÃO SERIALIZADA DAS REGRAS DE APROVAÇÃO

- **Comando ou inspeção:** criação da regressão `serializa a atualização das regras de aprovação sob lock da empresa` em `backend/src/modules/pts/pts.service.spec.ts:1534-1590`; execução pré-correção com `npm test -- --runTestsByPath src/modules/pts/pts.service.spec.ts --runInBand --no-cache --testNamePattern="serializa a atualização das regras"`; correção em `backend/src/modules/pts/pts.service.ts:2624-2654`; repetição do comando.
- **Evidência:** antes da correção, o teste rejeitou com `NotFoundException: Empresa não encontrada para configurar regras.` no caminho anterior, que não usava o manager transacional; depois, `1 suíte passada`, `1 teste passado` e `71 ignorados`. O método agora relê a empresa com `lock: { mode: 'pessimistic_write' }` e salva pelo repositório do mesmo transaction manager em `backend/src/modules/pts/pts.service.ts:2633-2652`.
- **Classificação:** `CONFIRMED` — lost update P1/P2 na configuração das regras de aprovação corrigido; alterações parciais concorrentes ficam serializadas por empresa.
- **Ação tomada:** menor mudança no serviço PT, sem alterar payload, endpoint ou defaults; falha de autorização/RLS em ambiente live continua fora da evidência local.

## GATE FINAL-RECHECK-7 — APÓS LOCK DO PDF, DTO DE ASSINATURAS E REGRAS DE APROVAÇÃO

- **Comando ou inspeção:** focused PDF `npm test -- --runTestsByPath src/modules/pts/pts.service.spec.ts --runInBand --no-cache --testNamePattern="PDF"`; focused DTO `npm test -- --runTestsByPath src/modules/pts/dto/replace-pt-signatures.dto.spec.ts --runInBand --no-cache`; focused regras `npm test -- --runTestsByPath src/modules/pts/pts.service.spec.ts --runInBand --no-cache --testNamePattern="serializa a atualização das regras"`; backend `npm run test:ci`, `npm run lint`, `npm run type-check`, `npm run build`, `npm audit --audit-level=high`; `git diff --check`; rechecagem de escopo e SHA.
- **Evidência:** focused PDF `1 suíte, 12 testes passados, 60 ignorados`; focused DTO `1 suíte, 1 teste`; focused regras `1 suíte, 1 teste, 71 ignorados`; suíte backend final `327/327` e `2860/2860`; lint, type-check e build terminaram exit `0`; audit retornou `found 0 vulnerabilities`; `STATUS_PATHS=35`, `APR_FILES=0`, `MIGRATION_FILES=0`, `WORKFLOW_FILES=0`, `VERIFY_NON_EOL_DIFF=0`, `git diff --check` exit `0` com apenas os avisos EOL históricos de `verify`; `HEAD=origin/main=5a5ae66ae8eb6410ade8a4b0e68dbd3105fc6219`.
- **Classificação:** `PASS` para os checks automatizados locais e para a fronteira de arquivos; `STATUS=PARTIAL` global permanece por ausência de banco/RLS/storage/auth live, E2E autenticado e validação visual interna completa.
- **Ação tomada:** nenhum arquivo APR, migration, workflow ou produção foi alterado; não houve commit, push, PR, merge ou deploy.

## GATE 3.4 — ETAPAS DO WIZARD COM SEMÂNTICA ACESSÍVEL

- **Comando ou inspeção:** teste de regressão `mantém o papel de botão nas etapas do wizard para navegação assistiva` em `frontend/app/dashboard/pts/components/PtForm.test.tsx:362-373`; execução focused antes da correção; ajuste mínimo em `frontend/app/dashboard/pts/components/PtForm.tsx:2038-2077`; repetição focused e suíte completa do `PtForm`.
- **Evidência:** antes, o focused falhou com `Unable to find an accessible element with role "button" and name /Etapa 1/`, porque o `<button>` tinha `role="listitem"`. Depois, o focused terminou `1 suíte passada, 1 teste passado, 13 ignorados` e a suíte completa do formulário terminou `1 suíte passada, 15 testes passados`. O contêiner agora é `role="listitem"` e o controle mantém semântica nativa de botão, `aria-label` e `aria-current="step"`.
- **Classificação:** `CONFIRMED` — defeito P1/P2 de navegação assistiva no wizard corrigido sem alterar router, state manager ou biblioteca visual.
- **Ação tomada:** separada a semântica de item de lista da semântica interativa do botão; adicionada regressão negativa/positiva. Nenhum APR, backend, migration, workflow ou produção foi alterado.

## GATE 3.5 — CARREGAMENTO DE APR CONDICIONADO À OBRA

- **Comando ou inspeção:** regressão `aguarda a obra antes de consultar APRs com escopo` em `frontend/app/dashboard/pts/components/PtForm.test.tsx:377-393`; execução pré-correção; correção em `frontend/app/dashboard/pts/components/PtForm.tsx:1512-1568`; repetição focused e suíte completa do `PtForm`.
- **Evidência:** antes, o teste falhou com `Expected number of calls: 0 / Received number of calls: 1`, mostrando `{ companyId: "company-1", limit: 100, page: 1 }` sem `siteId`, enquanto `frontend/src/services/aprsService.ts:363-367` exige `companyId e siteId`. Depois, o focused passou `1 suíte, 1 teste`, e a suíte PtForm passou `1 suíte, 15 testes`. O loader agora limpa APRs e aguarda a obra; quando disponível, envia `siteId` e reage à mudança de empresa/obra.
- **Classificação:** `CONFIRMED` — erro funcional P1/P2 e violação do contrato de escopo local corrigidos; evita toast/erro de console e evita consulta APR sem obra autorizada.
- **Ação tomada:** menor guarda de pré-condição e dependência do efeito; fixtures de teste passaram a simular seleção de obra após troca de empresa. Nenhum contrato backend foi inventado ou ampliado.

## GATE 3.6 — NAVEGADOR SINTÉTICO DO FORMULÁRIO PT

- **Comando ou inspeção:** servidor local com `NEXT_PUBLIC_API_URL=http://127.0.0.1:3011`, `NEXT_PUBLIC_APP_URL=http://127.0.0.1:3100`, `npm run dev -- --hostname 127.0.0.1 --port 3100`; `playwright-cli` headed com mocks locais de `/auth/*`, `/companies`, `/sites`, `/users`, `/aprs`, `/pts` e `/ai/*`; screenshots em `output/playwright/pt-form-final-desktop.png` e `output/playwright/pt-form-final-mobile.png`.
- **Evidência:** snapshot de `/dashboard/pts/new` exibiu `navigation`/`list`/`listitem` com botões nativos, campos e grupos de rádio; em 1440px: `viewport=1440`, `documentScrollWidth=1440`, `bodyScrollWidth=1440`, `mainScrollWidth=1166`, `mainClientWidth=1166`; em 390px: `viewport=390`, `documentScrollWidth=390`, `bodyScrollWidth=390`, `mainScrollWidth=380`, `mainClientWidth=380`. As imagens mostram o wizard e o formulário sem overflow horizontal em desktop/mobile. Após a correção do loader, não houve erro de APR; o único ruído restante foi WebSocket `ERR_CONNECTION_REFUSED` para `127.0.0.1:3011/socket.io`, porque o backend local não foi iniciado.
- **Classificação:** `PASS` para layout/semântica client-side sintética nos dois viewports; `EVIDENCE_LIMITED` para autenticação real, foco completo, reduced-motion, mutações, backend, RLS e storage. Não é evidência de produção.
- **Ação tomada:** nenhuma chamada remota mutável, credencial, dado real ou ambiente compartilhado utilizado; browser e servidor foram encerrados, não restou listener em 3100 e o script transitório de mock foi removido.

## GATE FINAL-RECHECK-8 — APÓS WIZARD, APR SCOPED LOADER E QA VISUAL SINTÉTICO

- **Comando ou inspeção:** frontend `npm run lint`; `npx tsc --noEmit --pretty false`; `npm run test:ci`; build com `NEXT_PUBLIC_API_URL=https://api.sgsseguranca.com.br` e `NEXT_PUBLIC_APP_URL=https://app.sgsseguranca.com.br`; `npm audit --audit-level=high`; `git diff --check`; rechecagem de escopo, SHA, listeners e sinks.
- **Evidência:** lint exit `0` com `PERMISSION_IMPORTS_OK` e Stylelint concluído; typecheck exit `0`; `test:ci` exit `0` com `1 skipped, 167 passed, 167 of 168 total` e `2 skipped, 913 passed, 915 total`; build Next `16.3.4` exit `0`, TypeScript concluído, `91/91` páginas geradas e `/dashboard/pts`, `/dashboard/pts/new`, `/dashboard/pts/edit/[id]` listadas; audit `found 0 vulnerabilities`; `STATUS_PATHS=35`, `APR_FILES=0`, `MIGRATION_FILES=0`, `WORKFLOW_FILES=0`, `VERIFY_NON_EOL_DIFF=0`, `PT_RUNTIME_DANGEROUS_SINKS=NONE`, `HEAD=origin/main=5a5ae66ae8eb6410ade8a4b0e68dbd3105fc6219`; `git diff --check` sem erro funcional, somente avisos CRLF históricos de `verify`; `NO_LISTENER_3100`.
- **Classificação:** `PASS` para checks automatizados locais, regressões PT e inspeção visual sintética; `STATUS=PARTIAL` global permanece por banco/RLS/storage/auth live, E2E autenticado real, foco/reduced-motion completos e contratos server-side não exercitados.
- **Ação tomada:** preservada a fronteira PT/testes/documentação; `frontend/CLAUDE.md` e `frontend/app/verify/*` foram mantidos como mudanças preexistentes/concorrentes; sem commit, push, PR, merge ou deploy.

## GATE 2.18 — CHAVES DE STORAGE DERIVADAS DO LOCK DA PT

- **Comando ou inspeção:** criação das regressões `gera a chave da foto usando a obra protegida pelo lock` e `gera a chave do anexo usando a obra protegida pelo lock` em `backend/src/modules/pts/pts.service.spec.ts:2170-2209,2580-2616`; execução pré-correção com `npm test -- --runInBand src/modules/pts/pts.service.spec.ts --testNamePattern="gera a chave"`; inspeção de `backend/src/modules/pts/pts.service.ts:1678-1796,1912-2030`; repetição focused e suíte backend completa.
- **Evidência:** antes da correção, os testes receberam `folderSegments: ["sites", "site-1"]` quando a linha protegida pelo lock estava em `site-2`; depois, o focused terminou `1 suíte passada, 2 testes passados, 72 ignorados`. Foto e anexo agora fazem validação, geração da chave, upload, mutação e persistência dentro de `executePtWorkflowTransition`, usando a obra lida com `FOR UPDATE`.
- **Classificação:** `CONFIRMED` — corrida P1/P2 de integridade e isolamento funcional em storage corrigida; uma troca concorrente de obra não grava o arquivo sob o prefixo stale da leitura anterior.
- **Ação tomada:** movido o upload governado para dentro do callback transacional e mantida compensação somente quando o upload ocorreu; o teste preexistente de compensação foi ajustado para falhar após o upload, refletindo o novo ponto correto de falha. Nenhum storage live, backend remoto ou produção foi acessado.

## GATE 2.19 — APR VINCULADA NO ESCOPO DA OBRA

- **Comando ou inspeção:** criação da regressão `bloqueia APR de outra obra ao criar a PT` em `backend/src/modules/pts/pts.service.spec.ts:829-866`; execução pré-correção com `npm test -- --runInBand src/modules/pts/pts.service.spec.ts --testNamePattern="bloqueia APR de outra obra"`; correção em `backend/src/modules/pts/pts.service.ts:511-530,603-655`; repetição focused e suíte backend completa.
- **Evidência:** antes da correção, a criação resolveu com `apr_id: "apr-site-2"` embora a PT estivesse em outra obra; depois, o focused terminou `1 suíte passada, 1 teste passado, 74 ignorados`. `assertAprScopedToSite` exige simultaneamente `id`, `company_id` e `site_id` antes de aceitar a relação.
- **Classificação:** `CONFIRMED` — IDOR/integridade P1/P2 intra-tenant corrigido no vínculo APR→PT; a filtragem da tela não é mais a única barreira contra associar APR de outra obra por request direto.
- **Ação tomada:** adicionada validação de escopo no serviço PT, sem inventar endpoint ou alterar APR; autorização efetiva do tenant/RLS continua dependente do contrato backend e banco live não exercitados nesta worktree.

## GATE FINAL-RECHECK-9 — APÓS STORAGE LOCKED UPLOAD E APR SITE SCOPE

- **Comando ou inspeção:** `npm test -- --runInBand src/modules/pts/pts.service.spec.ts`; `npm run test:ci`; `npm run lint`; `npm run type-check`; `npm run build`; `npm audit --audit-level=high`; `git diff --check`; `git status --short`; contagem de caminhos APR/migrations/workflows.
- **Evidência:** suíte PT `1 suíte passada, 75 testes passados`; backend `test:ci` `327/327` suítes e `2863/2863` testes passados, lint exit `0`, type-check exit `0`, build exit `0`, audit `found 0 vulnerabilities`; o recheck frontend já registrado no Gate Final-Recheck-8 permanece `167/168` suítes e `913/915` testes, lint/typecheck/build/audit verdes. A fronteira continua sem arquivos APR, migration ou workflow alterados; os únicos avisos de `git diff --check` são os CRLF históricos de `frontend/app/verify/page.tsx` e `page.test.tsx`, cujo diff funcional ignorando EOL é zero.
- **Classificação:** `PASS` para a implementação PT e todos os checks automatizados locais executáveis; `STATUS=PARTIAL` global permanece por autenticação real, banco/RLS/storage live, contratos server-side externos e foco/reduced-motion completos no navegador.
- **Ação tomada:** nenhuma chamada remota mutável, credencial, migração compartilhada, produção, commit, push, PR, merge ou deploy; `frontend/CLAUDE.md` e os arquivos `verify` foram preservados como mudanças preexistentes/concorrentes.

## GATE FINAL-RECHECK-10 — SUÍTE CONSOLIDADA E DRIFT DA MAIN

- **Comando ou inspeção:** frontend `npm run test:ci`, `npm run lint`, `npx tsc --noEmit --pretty false`, build com `NEXT_PUBLIC_API_URL=https://api.sgsseguranca.com.br` e `NEXT_PUBLIC_APP_URL=https://app.sgsseguranca.com.br`, `npm audit --audit-level=high`; backend `npm run test:ci`, `npm run lint`, `npm run type-check`, `npm run build`, `npm audit --audit-level=high`; `git diff --check`; varredura de sinks e blob lifecycle; `git fetch origin`; comparação `AUDIT_START_MAIN_SHA..AUDIT_END_MAIN_SHA`.
- **Evidência:** frontend atual `1 suíte skipped, 167 passed, 167 of 168 total`; `2 skipped, 913 passed, 915 total`; lint exit `0` com `PERMISSION_IMPORTS_OK`/Stylelint, typecheck exit `0`, build Next `16.3.4` com `91/91` páginas e audit `found 0 vulnerabilities`. Backend atual `327/327` suítes e `2863/2863` testes, lint/type-check/build exit `0` e audit `found 0 vulnerabilities`. `git diff --check` exit `0` com apenas avisos CRLF históricos de `verify`; `PT_RUNTIME_DANGEROUS_SINKS` sem matches; `URL.createObjectURL` possui revogação correspondente; `NO_LISTENER_3100`. Drift: `AUDIT_START_MAIN_SHA=5a5ae66ae8eb6410ade8a4b0e68dbd3105fc6219`, `AUDIT_END_MAIN_SHA=5a5ae66ae8eb6410ade8a4b0e68dbd3105fc6219`, `MAIN_DRIFT=NONE`.
- **Classificação:** `PASS` para a implementação local e a reconciliação de SHA; `STATUS=PARTIAL` global permanece por `AUTH_E2E_RUNTIME_EVIDENCE_BLOCKED`, banco/RLS/storage live, contratos server-side não exercitados e foco/reduced-motion completos não provados.
- **Ação tomada:** nenhum rebase, merge, commit, push, PR ou deploy; nenhuma alteração em APR, migration, workflow, produção, VPS ou dados reais. O resultado permanece restrito à worktree isolada.

## GATE 2.20 — REGRAS DE APROVAÇÃO CONSISTENTES COM O LOCK DA EMPRESA

- **Comando ou inspeção:** criação da regressão `lê regras de aprovação pelo manager transacional sob lock da empresa` em `backend/src/modules/pts/pts.service.spec.ts:1703-1768`; execução pré-correção com `npm test -- --runInBand src/modules/pts/pts.service.spec.ts --testNamePattern="lê regras de aprovação pelo manager"`; correção em `backend/src/modules/pts/pts.service.ts:1540,2707-2713,2890-2902`; repetição focused, suíte PT, suíte backend completa, lint, type-check e build.
- **Evidência:** antes da correção, o teste falhou com `Received promise resolved instead of rejected`: a aprovação usou regras permissivas do repositório global mesmo quando o manager transacional continha regra restritiva para risco crítico. Depois, o focused passou `1 teste passado, 75 ignorados`; a suíte PT passou `76/76`; a suíte backend passou `327/327` suítes e `2864/2864` testes.
- **Classificação:** `CONFIRMED` — corrida P1 de consistência de autorização: uma alteração concorrente das regras da empresa podia não ser observada pela aprovação e permitir decisão baseada em política stale.
- **Ação tomada:** `approve()` passa o manager ao avaliador; a leitura de `Company` usa `pessimistic_read` dentro da mesma transação, serializando-se com `updateApprovalRules()` que usa `pessimistic_write`. Nenhum contrato externo, migration, APR, produção ou banco live foi alterado.

## GATE FINAL-RECHECK-11 — APÓS CONSISTÊNCIA TRANSACIONAL DAS REGRAS

- **Comando ou inspeção:** backend `npm run test:ci`, `npm run lint`, `npm run type-check`, `npm run build`, `npm audit --audit-level=high`; focused PT `npm test -- --runInBand src/modules/pts/pts.service.spec.ts`; `git diff --check`; verificação de escopo e SHA.
- **Evidência:** backend `327/327` suítes e `2864/2864` testes passados; suíte PT `76/76`; lint/type-check/build exit `0`; audit `found 0 vulnerabilities`; `git diff --check` exit `0` com somente os avisos CRLF históricos dos arquivos `verify`; `APR_FILES=0`, `MIGRATION_FILES=0`, `WORKFLOW_FILES=0`; `HEAD=origin/main=5a5ae66ae8eb6410ade8a4b0e68dbd3105fc6219`; nenhuma porta local PT permaneceu aberta.
- **Classificação:** `PASS` para o código e os checks automatizados locais; `STATUS=PARTIAL` global permanece por E2E autenticado, RLS/storage live, contratos server-side externos e validação completa de foco/reduced-motion.
- **Ação tomada:** nenhuma alteração em APR, migrations, workflows ou produção; sem commit, push, PR, merge ou deploy.

## GATE 2.21 — SNAPSHOT DE AUDITORIA DERIVADO DA PT PROTEGIDA

- **Comando ou inspeção:** criação da regressão `registra o snapshot anterior da aprovação a partir da PT protegida pelo lock` em `backend/src/modules/pts/pts.service.spec.ts:1771-1842`; execução pré-correção com `npm test -- --runInBand src/modules/pts/pts.service.spec.ts --no-cache --testNamePattern="registra o snapshot anterior da aprovação"`; correção em `backend/src/modules/pts/pts.service.ts:1515-1634`; repetição focused e suíte completa do serviço PT.
- **Evidência:** antes da correção, o teste falhou com `Expected ... before.titulo: "Título atual"` e recebeu `before.titulo: "Título antigo"`, demonstrando que `approve()` auditava uma leitura fora do lock. Depois, o focused passou `1 teste passado, 76 ignorados` e a suíte PT passou `77/77`. `approve`, `reject` e `finalize` agora capturam `before = { ...pt }` dentro do callback de `executePtWorkflowTransition`, após o `SELECT ... FOR UPDATE NOWAIT`.
- **Classificação:** `CONFIRMED` — inconsistência P2 de trilha forense corrigida; decisões de auditoria das três transições não usam mais snapshot obtido antes da serialização da PT.
- **Ação tomada:** movida somente a captura do snapshot para dentro do lock, preservando o contrato das transições e sem alterar APR, migrations, workflows, produção ou armazenamento remoto.

## GATE FINAL-RECHECK-12 — CONSOLIDAÇÃO APÓS SNAPSHOT FORENSE

- **Comando ou inspeção:** backend `npm run test:ci`, `npm run lint`, `npm run type-check`, `npm run build`, `npm audit --audit-level=high`; frontend `npm run test:ci`, `npm run lint`, `npx tsc --noEmit --pretty false`, build com `NEXT_PUBLIC_API_URL=https://api.sgsseguranca.com.br` e `NEXT_PUBLIC_APP_URL=https://app.sgsseguranca.com.br`, `npm audit --audit-level=high`; focused PT `npm test -- --runInBand src/modules/pts/pts.service.spec.ts --no-cache`; rechecagem de fronteira, sinks, blob lifecycle, EOL e SHA.
- **Evidência:** backend `327/327` suítes e `2865/2865` testes passados; serviço PT `77/77`; backend lint/type-check/build exit `0` e audit `found 0 vulnerabilities`. Frontend `1 suíte skipped, 167 passed, 167 of 168 total`; `2 skipped, 913 passed, 915 total`; lint/type-check/build exit `0`, Next `16.3.4` gerou `91/91` páginas e audit `found 0 vulnerabilities`. Rechecagem: `STATUS_PATHS=35`, `APR_FILES=0`, `MIGRATION_FILES=0`, `WORKFLOW_FILES=0`, `VERIFY_NON_EOL_DIFF=0`, `PT_RUNTIME_DANGEROUS_SINKS=NONE`, blob URLs com `URL.revokeObjectURL`, `HEAD=ORIGIN_MAIN=5a5ae66ae8eb6410ade8a4b0e68dbd3105fc6219`, `MAIN_DRIFT=NONE`, `NO_LISTENER_3100`. `git diff --check` exit `0`, com somente os avisos históricos de CRLF em `frontend/app/verify/page.tsx` e `page.test.tsx`.
- **Classificação:** `PASS` para código PT, testes e checks locais executáveis; `STATUS=PARTIAL` global permanece por E2E autenticado, banco/RLS/storage live, contratos server-side externos e validação completa de foco/reduced-motion.
- **Ação tomada:** nenhuma alteração em APR, migrations, workflows, produção ou branches compartilhados; sem commit, push, PR, merge ou deploy.

## GATE 2.22 — PAGINAÇÃO COMPLETA DA ALLOW-LIST DE ARQUIVOS

- **Comando ou inspeção:** criação da regressão `pagina a allow-list de PTs ao filtrar arquivos armazenados` em `backend/src/modules/pts/pts.service.spec.ts:1576-1604`; execução pré-correção com `npm test -- --runInBand src/modules/pts/pts.service.spec.ts --no-cache --testNamePattern="pagina a allow-list"`; correção em `backend/src/modules/pts/pts.service.ts:113,730-778`; repetição focused, lint do escopo PT e suíte do serviço.
- **Evidência:** antes da correção, o teste falhou e retornou somente `pt-0`, omitindo `pt-1000` após a primeira página; depois, o focused passou `1 teste passado, 77 ignorados`, a suíte PT passou `78/78` e o lint focused terminou `exit 0`. A allow-list agora usa lotes ordenados por `id ASC` e `MoreThan(lastId)` até o último lote, sem o teto arbitrário `take: 50000`.
- **Classificação:** `CONFIRMED` — disponibilidade/integridade P2 na listagem de PDFs autorizados corrigida; documentos além do teto anterior não desaparecem da filtragem local.
- **Ação tomada:** substituído somente o teto fixo por paginação keyset no serviço PT; mantidos company/site scope, `deleted_at IS NULL`, document registry e contratos externos. Nenhum APR, migration, workflow, storage live ou produção foi alterado.

## GATE FINAL-RECHECK-13 — CONSOLIDAÇÃO APÓS PAGINAÇÃO DA ALLOW-LIST

- **Comando ou inspeção:** backend `npm run test:ci`, `npm run lint`, `npm run type-check`, `npm run build`, `npm audit --audit-level=high`; focused PT `npm test -- --runInBand src/modules/pts/pts.service.spec.ts --no-cache`; rechecagem de fronteira, sinks, blob lifecycle, EOL e SHA.
- **Evidência:** backend `327/327` suítes e `2866/2866` testes passados; focused PT `78/78`; lint/type-check/build exit `0`; audit `found 0 vulnerabilities`. Rechecagem: `STATUS_PATHS=35`, `APR_FILES=0`, `MIGRATION_FILES=0`, `WORKFLOW_FILES=0`, `VERIFY_NON_EOL_DIFF=0`, `PT_RUNTIME_DANGEROUS_SINKS=NONE`, blob URLs com revogação, `HEAD=ORIGIN_MAIN=5a5ae66ae8eb6410ade8a4b0e68dbd3105fc6219`, `MAIN_DRIFT=NONE`, `NO_LISTENER_3100`. Frontend permanece validado no Gate Final-Recheck-12 com `167/168` suítes, `913/915` testes, lint/typecheck/build/audit verdes; `git diff --check` exit `0`, com apenas avisos históricos de CRLF em `frontend/app/verify/page.tsx` e `page.test.tsx`.
- **Classificação:** `PASS` para código PT, testes e checks locais executáveis; `STATUS=PARTIAL` global permanece por E2E autenticado, banco/RLS/storage live, contratos server-side externos e validação completa de foco/reduced-motion.
- **Ação tomada:** nenhuma alteração em APR, migrations, workflows, produção ou branches compartilhados; sem commit, push, PR, merge ou deploy.

## GATE 2.23 — BLOQUEIO DOS ENDPOINTS GENÉRICOS DE MUTAÇÃO DE ASSINATURA PT

- **Comando ou inspeção:** criação das regressões `recusa assinatura de PT pelo endpoint genérico`, `recusa remoção genérica de assinatura de PT` e `recusa substituição genérica de assinaturas de PT` em `backend/src/modules/signatures/signatures.service.spec.ts:317-365`; execução pré-correção com `npm test -- --runInBand src/modules/signatures/signatures.service.spec.ts --no-cache --testNamePattern="PT pelo endpoint|remoção genérica|substituição genérica"`; correção em `backend/src/modules/signatures/signatures.service.ts:189,294,700,736,2050-2062`; repetição focused e suíte compartilhada.
- **Evidência:** antes da correção, os três testes falharam recebendo `Documento não encontrado para assinaturas.` em vez do bloqueio específico, demonstrando que os caminhos genéricos alcançavam a resolução da PT. Depois, focused `3 testes passados, 39 ignorados`; suíte `SignaturesService` `42/42`. O guard fail-closed força `POST/DELETE /signatures` a usar `POST/PUT /pts/:id/signatures`, onde há validação de executante e lock da PT; DDS/APR permanecem nos caminhos genéricos existentes.
- **Classificação:** `CONFIRMED` — bypass P1 de autorização/integridade de assinatura PT corrigido; não existe mais rota genérica de mutação que contorne o contrato específico da PT.
- **Ação tomada:** guard centralizado no serviço compartilhado para criação, remoção e replacement genéricos de PT; `createWithManager()` interno continua disponível ao serviço PT. Nenhuma migration, APR, workflow ou produção foi alterada.

## GATE FINAL-RECHECK-14 — CONSOLIDAÇÃO APÓS BLOQUEIO DE ASSINATURAS GENÉRICAS

- **Comando ou inspeção:** backend `npm run test:ci`, `npm run lint`, `npm run type-check`, `npm run build`, `npm audit --audit-level=high`; focused PT `npm test -- --runInBand src/modules/pts/pts.service.spec.ts --no-cache`; suíte `SignaturesService`; frontend recheck do estado atual; fronteira, sinks, blob lifecycle, EOL e SHA.
- **Evidência:** backend `327/327` suítes e `2869/2869` testes passados; lint/type-check/build exit `0`; audit `found 0 vulnerabilities`; PT `78/78`; assinaturas `42/42`. Frontend permanece em `167/168` suítes, `913/915` testes, lint/typecheck/build/audit verdes, com `91/91` páginas geradas. Rechecagem: `STATUS_PATHS=37`, `APR_FILES=0`, `MIGRATION_FILES=0`, `WORKFLOW_FILES=0`, `VERIFY_NON_EOL_DIFF=0`, sinks perigosos PT ausentes, blob URLs revogadas, `HEAD=ORIGIN_MAIN=5a5ae66ae8eb6410ade8a4b0e68dbd3105fc6219`, `MAIN_DRIFT=NONE`, `NO_LISTENER_3100`.
- **Classificação:** `PASS` para código PT, contratos locais e checks automatizados executáveis; `STATUS=PARTIAL` global permanece por E2E autenticado, banco/RLS/storage live, contratos server-side externos e validação completa de foco/reduced-motion.
- **Ação tomada:** nenhuma alteração em APR, migrations, workflows, produção ou branches compartilhados; sem commit, push, PR, merge ou deploy.

## GATE 2.24 — PAGINAÇÃO COMPLETA DO EXPORT LEGADO DE PT

- **Comando ou inspeção:** criação da regressão `deve continuar a exportação depois da primeira página` em `backend/src/modules/pts/pts.service.pagination.spec.ts:227-254`; execução pré-correção com `npm test -- --runInBand src/modules/pts/pts.service.pagination.spec.ts --no-cache --testNamePattern="continuar a exportação"`; correção em `backend/src/modules/pts/pts.service.ts:928-990`; repetição focused com `npm test -- --runInBand src/modules/pts/pts.service.pagination.spec.ts --no-cache --testNamePattern="continuar a exportação|limitar a 5000"`.
- **Evidência:** antes da correção, a regressão recebeu `Expected length: 5001 / Received length: 5000`, demonstrando o truncamento silencioso do endpoint `/pts/export/all`. Depois, o focused terminou `1 suíte passada`, `2 testes passados`, `6 ignorados`. O método mantém páginas de 5.000 para limitar memória por consulta, mas percorre todas as páginas com ordenação determinística `created_at DESC, id DESC` e cursor composto.
- **Classificação:** `CONFIRMED` — perda de disponibilidade/integridade P2 na exportação legada corrigida; o escopo de empresa/obra e `deleted_at IS NULL` continuam aplicados em todas as páginas.
- **Ação tomada:** substituído o teto único por paginação keyset, preservando o retorno `Pt[]`, o endpoint, os campos selecionados e os contratos de tenant; nenhum APR, migration, workflow, produção ou storage live foi alterado.

## GATE 2.25 — INPUTS, EXPORTAÇÕES E LEITURA DE ANEXOS

- **Comando ou inspeção:** leitura de `backend/src/main.ts:353-365`, `backend/src/modules/pts/dto/create-pt.dto.ts:22-333`, `backend/src/modules/pts/pts.service.ts:924-990,1823-1874,2082-2134` e `frontend/app/dashboard/pts/components/pt-schema-and-data.ts:348-467`; busca de uso de `/pts/export/all` e dos fluxos de acesso de foto/anexo.
- **Evidência:** o `ValidationPipe` usa `whitelist`, `forbidNonWhitelisted` e `transform`, com limite global de corpo de 5 MB; os arrays do PT têm limites declarativos e upload separado de 10 MB com inspeção de arquivo. O export Excel (`backend/src/modules/pts/pts.service.ts:2603-2644`) não usa o endpoint legado nem o teto de 5.000. A leitura de foto/anexo por índice (`:1823-1874` e `:2082-2134`) captura o recurso dentro da PT já autorizada; remoções são serializadas por lock (`:1876-1935`).
- **Classificação:** `PASS` estático para limites de corpo/arrays, autorização local, allowlist e limpeza; `INFERRED` residual para a corrida funcional entre leitura por índice e remoção concorrente. Resolver essa corrida exigiria trocar índices por IDs estáveis ou manter lock durante I/O de storage, com custo/contrato incompatível com uma correção mínima. Não foi promovido a P0/P1 porque não amplia tenant, autorização ou acesso a recurso de outra PT.
- **Ação tomada:** nenhuma alteração adicional; limites e contratos já existentes foram preservados. A validação live de autenticação, RLS e storage continua registrada como `BACKEND_CONTRACT_BLOCKER`.

## GATE FINAL-RECHECK-15 — VALIDAÇÃO SERIAL APÓS PAGINAÇÃO DE EXPORTAÇÃO

- **Comando ou inspeção:** suíte PT `npm test -- --runInBand src/modules/pts --no-cache`; backend `npm run test:ci`, `npm run lint`, `npm run type-check`, `npm run build`, `npm audit --audit-level=high`; frontend `npm run test:ci`, `npm run lint`, `npx tsc --noEmit --pretty false`, build com `NEXT_PUBLIC_API_URL=https://api.sgsseguranca.com.br` e `NEXT_PUBLIC_APP_URL=https://app.sgsseguranca.com.br`, `npm audit --audit-level=high`; rechecagem de status, SHA, fronteira, EOL, sinks, blob lifecycle, `git diff --check` e listener 3100.
- **Evidência:** suíte PT `7/7` e `120/120`; backend `327/327` suítes e `2870/2870` testes passados; backend lint/type-check/build exit `0` e audit `found 0 vulnerabilities`. Frontend `167/168` suítes e `913/915` testes passados, com `1` suíte e `2` testes ignorados; lint exit `0` com `PERMISSION_IMPORTS_OK`/Stylelint, typecheck exit `0`, build Next `16.3.4` com `91/91` páginas, audit `found 0 vulnerabilities`. Rechecagem: `STATUS_PATHS=38`, `APR_FILES=0`, `MIGRATION_FILES=0`, `WORKFLOW_FILES=0`, `VERIFY_NON_EOL_DIFF=0`, `PT_RUNTIME_DANGEROUS_SINKS=NONE`, blob URLs com revogação, `HEAD=ORIGIN_MAIN=5a5ae66ae8eb6410ade8a4b0e68dbd3105fc6219`, `MAIN_DRIFT=NONE`, `DIFF_CHECK_EXIT=0`, `NO_LISTENER_3100`.
- **Classificação:** `PASS` para os gates automatizados locais e para a fronteira de arquivos; `STATUS=PARTIAL` global permanece por E2E autenticado, banco/RLS/storage live, contratos server-side externos e validação completa de foco/reduced-motion. Uma tentativa paralela de quatro processos gerou OOM local; os mesmos gates foram repetidos serialmente e passaram, sem evidência de falha funcional.
- **Ação tomada:** nenhuma alteração em APR, migrations, workflows, produção ou branches compartilhados; nenhuma chamada remota mutável, commit, push, PR, merge ou deploy.

## GATE 2.26 — LIMITES DE ENTRADA E FILTROS DO PT

- **Comando ou inspeção:** leitura dos DTOs/schema e dos filtros de arquivos; regressões `ApprovePtDto`, `create-pt.dto`, `pts.controller` e schema frontend; focused `npm test -- --runInBand --no-cache` nos três arquivos backend e `npm test -- --runInBand --runTestsByPath app/dashboard/pts/components/ChecklistSection.test.tsx app/dashboard/pts/components/RapidRiskAnalysisSection.test.tsx app/dashboard/pts/components/PtRejectModal.test.tsx app/dashboard/pts/components/pt-schema-and-data.test.ts app/dashboard/pts/components/PtForm.test.tsx --no-cache`.
- **Evidência:** `backend/src/modules/pts/dto/create-pt.dto.ts:24-54,92-126,140-150,179-181,237-351` agora limita textos, arrays e IDs; `ApprovePtDto` aplica `@MaxLength(2000)` em `backend/src/modules/pts/dto/approve-pt.dto.ts:1-8`; o schema e os controles do frontend refletem limites em `frontend/app/dashboard/pts/components/pt-schema-and-data.ts:340-406`, `BasicInfoSection.tsx:96,140,160`, `ChecklistSection.tsx:220-241`, `EmergencyRescueSection.tsx:82,101,117,178`, `RapidRiskAnalysisSection.tsx:154-178` e `PtRejectModal.tsx:54-65`. Antes da correção, o teste do motivo retornou `Expected value: "reason" / Received array: []`; o filtro `year=abc&week=99` retornou `200 OK` em vez de `400 Bad Request`. Depois: DTO de criação `1 suíte, 4 testes`; DTO de aprovação `1 suíte, 1 teste`; controller `1 suíte, 11 testes`; frontend focused `5 suítes, 27 testes`, todos exit `0`. O teste de volume do DTO de criação foi acrescentado e passou após a implementação; não foi capturada uma execução red separada desse caso.
- **Classificação:** `CONFIRMED` — validação P2 de limite de payload e filtro numérico inválido corrigida no contrato do PT; o frontend também impede entrada textual acima dos limites principais e expõe os erros aos leitores de tela. Não há alteração de contrato de banco nem relaxamento de autorização.
- **Ação tomada:** adicionados limites declarativos compatíveis nos DTOs/schema e `maxLength` nos controles principais; ano e semana ISO agora rejeitam valores não inteiros ou fora do intervalo em `backend/src/modules/pts/pts.controller.ts:60-88,286-299`. Nenhuma alteração em APR, migrations, workflows ou produção.

## GATE 2.27 — CONSISTÊNCIA DAS MÉTRICAS APÓS EXPIRAÇÃO

- **Comando ou inspeção:** regressão deferred `aguarda a atualização de PTs expiradas antes de calcular métricas` em `backend/src/modules/pts/pts.service.spec.ts:1549-1581`; execução pré-correção com `node --max-old-space-size=4096 node_modules/jest/bin/jest.js --runInBand --testPathPatterns=src/modules/pts/pts.service.spec.ts --testNamePattern="aguarda a atualização" --no-cache`; repetição focused após a correção.
- **Evidência:** antes, `expect(countMock).not.toHaveBeenCalled()` falhou com `Expected 0, Received 6`, mostrando que as contagens podiam observar a PT antes do refresh de expiração. Depois, o focused passou `1 suíte`, `1 teste passado`, `78 testes ignorados`, e `getAnalyticsOverview()` aguarda `refreshExpiredStatuses(companyId)` em `backend/src/modules/pts/pts.service.ts:2679-2700`, mantendo o log de erro e as contagens com `deleted_at: IsNull()`/escopo de obra.
- **Classificação:** `CONFIRMED` — inconsistência P2 de leitura das métricas corrigida; falha do refresh continua observável e não é convertida em sucesso silencioso de mutação.
- **Ação tomada:** somente o caminho de analytics passou a aguardar o refresh; listagens e demais fluxos preservam o comportamento assíncrono existente. Nenhum backend externo, banco live, migration ou produção foi tocado.

## GATE FINAL-RECHECK-16 — FECHAMENTO APÓS LIMITES, FILTROS E MÉTRICAS

- **Comando ou inspeção:** backend `npm run test:ci`, `npm run lint`, `npm run type-check`, `npm run build`, `npm audit --audit-level=high`; frontend `npm run test:ci`, `npm run lint`, `npx tsc --noEmit --pretty false`, build com `NEXT_PUBLIC_API_URL=https://api.sgsseguranca.com.br` e `NEXT_PUBLIC_APP_URL=https://app.sgsseguranca.com.br`, `npm audit --audit-level=high`; `git fetch origin`; `git diff --check`; comparação de SHA; varredura de sinks, blob lifecycle, EOL e listener local.
- **Evidência:** backend `328/328` suítes e `2875/2875` testes passados, lint/type-check/build exit `0`, audit `found 0 vulnerabilities`; frontend `167/168` suítes, `914/916` testes, com `1` suíte e `2` testes ignorados, lint exit `0` com `PERMISSION_IMPORTS_OK`/Stylelint, typecheck exit `0`, build Next `16.3.4` com `91/91` páginas e audit `found 0 vulnerabilities`. A rechecagem retornou `VERIFY_NON_EOL_DIFF=0`, `PT_RUNTIME_DANGEROUS_SINKS=NONE`, `NO_LISTENER_3100`, `STATUS_PATHS=46`, `APR_FILES=0`, `MIGRATION_FILES=0`, `WORKFLOW_FILES=0`, `HEAD=ORIGIN_MAIN=5a5ae66ae8eb6410ade8a4b0e68dbd3105fc6219` e `git diff --check` exit `0`; os únicos avisos são os CRLF históricos de `frontend/app/verify/page.tsx` e `page.test.tsx`.
- **Classificação:** `PASS` para o código PT e todos os checks automatizados locais executáveis; `STATUS=PARTIAL` global permanece por E2E autenticado com credenciais, banco/RLS/storage live, contratos server-side externos e validação completa de foco/reduced-motion no navegador. A ausência de APR/migration/workflow no diff e o drift `NONE` foram confirmados.
- **Ação tomada:** nenhum commit, push, PR, merge ou deploy; nenhuma chamada remota mutável, alteração de produção, migration compartilhada ou processo concorrente do APR foi interrompido. Worktree isolada preservada para revisão/aprovação.

## GATE 2.28 — RLS DE ESCOPO DE OBRA FAIL-CLOSED

- **Comando ou inspeção:** leitura comparativa das migrations `backend/src/infra/database/migrations/1709000000127-harden-site-scoped-tenant-rls.ts:20-35,78-91`, `1709000000367-enable-multi-site-rls-scope.ts:29-46,128-143` e da injeção de contexto em `backend/src/shared/database/tenant-db-context.service.ts:227-267`; criação da regressão estática em `backend/src/modules/pts/pts-migrations.spec.ts:158-181`; execução pré-correção `npm test -- --runInBand src/modules/pts/pts-migrations.spec.ts --no-cache --testNamePattern="current_site_scope fail-closed"`.
- **Evidência:** antes, o teste falhou com `ENOENT` porque `1709000000403-harden-site-scope-fail-closed.ts` não existia. A migration histórica `0127` retornava `'all'` para contexto ausente, vazio ou inválido, e as policies multi-site tratavam `'all'` como acesso global dentro do tenant. Depois, `backend/src/infra/database/migrations/1709000000403-harden-site-scope-fail-closed.ts:20-45` normaliza somente `all`/`single` e retorna `'single'` em ausência, valor desconhecido ou exceção; o focused passou `1 suíte, 1 teste passado, 20 ignorados`. O manifesto também passou com `npm run ci:migration:check`: `Migration manifest passed (323 files; 20 names derived from class)`.
- **Classificação:** `CONFIRMED` — falha P0 de isolamento por escopo de obra no RLS corrigida no código de migration. `BACKEND_CONTRACT_BLOCKER`: a migration e a policy precisam ser aplicadas e exercitadas no banco de QA; nenhum banco compartilhado foi alterado.
- **Contrato backend esperado/observado/risco:** esperado: conexão sem `app.current_site_scope` válido nunca deve ampliar o acesso; usuário restrito deve ler/escrever somente `site_id` pertencente a `app.current_site_ids`, com `company_id` derivado do contexto confiável. Observado: o pool HTTP injeta `single`/`all` e a migration corretiva fecha fallback inválido, mas a execução live de RLS não foi realizada. Risco: ambiente com migration pendente mantém a exposição histórica até a aplicação controlada da migration.
- **Ação tomada:** criada somente a migration corretiva `0403`, sem editar migrations históricas e sem tocar APR, workflows ou produção; `down()` não reintroduz o fallback inseguro.

## GATE 2.29 — VALIDAÇÃO DE RELAÇÕES DENTRO DA TRANSAÇÃO PROTEGIDA

- **Comando ou inspeção:** regressão `valida entidades relacionadas usando o manager da transação protegida` em `backend/src/modules/pts/pts.service.spec.ts:941-970`; execução pré-correção `node --max-old-space-size=4096 node_modules/jest/bin/jest.js --runInBand --testPathPatterns=src/modules/pts/pts.service.spec.ts --testNamePattern="manager da transação protegida" --no-cache`; inspeção de `backend/src/modules/pts/pts.service.ts:496-626,1248-1280`.
- **Evidência:** antes, o teste recebeu uma única chamada de `validateRelatedEntityScope` sem o segundo argumento e falhou com `Expected ... Anything / Received ... Number of calls: 1`, mostrando que a checagem usava o manager/repository global fora do lock. Depois, `assertCompanyScopedEntityId`, `assertAprScopedToSite`, `assertCompanyScopedEntityIds` e `assertUsersScopedToSite` aceitam o manager opcional e `update()` o repassa; o focused passou `1 suíte, 1 teste passado, 79 ignorados`. A suíte PT posterior passou `101/101` e a suíte backend final passou `328/328`.
- **Classificação:** `CONFIRMED` — corrida P1 de integridade/autorização intra-tenant corrigida; a leitura de site, APR e usuários relacionada ao `update()` usa o mesmo contexto transacional da PT bloqueada.
- **Contrato backend esperado/observado/risco:** esperado: relações de uma PT sejam validadas contra `company_id`/`site_id` da linha protegida e no mesmo fluxo transacional antes do save. Observado: o serviço local implementa e testa o repasse; RLS/constraints live não foram exercitados. Risco: alterações de autorização externa, deploy incompleto ou dados inconsistentes no banco remoto continuam fora da evidência local.
- **Ação tomada:** propagação mínima do `EntityManager` nos helpers de escopo; nenhuma troca de ORM, router, state manager ou contrato público.

## GATE 2.30 — LIMITE DE REFERÊNCIAS LEGADAS DE EVIDÊNCIA

- **Comando ou inspeção:** regressão em `backend/src/modules/pts/dto/create-pt.dto.spec.ts:104-114`; execução pré-correção `npm test -- --runInBand src/modules/pts/dto/create-pt.dto.spec.ts --no-cache --testNamePattern="referências legadas"`; correção em `backend/src/modules/pts/dto/create-pt.dto.ts:227-235`; repetição focused.
- **Evidência:** antes, o teste recebeu `Received: []` para strings de 100.001 caracteres, sem erro de validação. Depois, `evidence_photo` e `evidence_document` usam `@MaxLength(100_000)` e o focused passou `1 suíte, 1 teste passado, 4 ignorados`. O fluxo atual de foto/anexo usa upload multipart governado e não depende desses campos legados.
- **Classificação:** `CONFIRMED` — consumo P2 de payload/armazenamento por campo legado sem limite próprio corrigido; não há relaxamento de autorização ou mudança de storage.
- **Ação tomada:** adicionados limites declarativos mínimos nos dois campos, preservando compatibilidade com referências normais e o limite global de corpo de 5 MB.

## GATE FINAL-RECHECK-17 — FECHAMENTO APÓS RLS, MANAGER E DTO

- **Comando ou inspeção:** backend `npm run test:ci`, `npm run lint`, `npm run type-check`, `npm run build`, `npm audit --audit-level=high`, `npm run ci:migration:check`; frontend `npm run test:ci`, `npm run lint`, `npx tsc --noEmit --pretty false`, build com `NEXT_PUBLIC_API_URL=https://api.sgsseguranca.com.br` e `NEXT_PUBLIC_APP_URL=https://app.sgsseguranca.com.br`, `npm audit --audit-level=high`; `git diff --check`; `git fetch origin`; comparação de SHA; contagem de fronteira, sinks, EOL e listener local.
- **Evidência:** backend `test:ci` exit `0`, `328/328` suítes e `2878/2878` testes passados; lint/type-check/build exit `0`; audit `found 0 vulnerabilities`; manifesto `323 files` passou. Frontend `test:ci` exit `0`, `1 suíte skipped, 167 passed, 167 de 168` e `2 skipped, 914 passed, 916 total`; lint exit `0` com `PERMISSION_IMPORTS_OK`/Stylelint, typecheck exit `0`, build Next `16.3.4` exit `0` com `91/91` páginas, audit `found 0 vulnerabilities`. `STATUS_PATHS=49`, `APR_FILES=0`, `MIGRATION_FILES=1` (`1709000000403-harden-site-scope-fail-closed.ts`), `WORKFLOW_FILES=0`, `VERIFY_NON_EOL_DIFF=0`, `PT_RUNTIME_DANGEROUS_SINKS=NONE`, `NO_LISTENER_3100`; o `git diff --check` só emite os avisos CRLF históricos dos dois arquivos `verify`. `HEAD=origin/main=5a5ae66ae8eb6410ade8a4b0e68dbd3105fc6219`, `MAIN_DRIFT=NONE`.
- **Classificação:** `PASS` para os checks automatizados locais executáveis e para os gates P0/P1/P2 do módulo PT fechados no código; `STATUS=PARTIAL` global por autenticação E2E real, banco/RLS/storage live, contratos backend externos e validação completa de foco/reduced-motion. A execução de `npm ci` é a baseline registrada no Gate 0.1; não foi repetida após mudanças porque não houve alteração de lockfile/dependência.
- **Ação tomada:** nenhum commit, push, PR, merge, deploy, migration compartilhada ou acesso à produção; preservadas as mudanças concorrentes/preexistentes em `frontend/CLAUDE.md` e `frontend/app/verify/*`. Worktree permanece local para revisão explícita.

## GATE 2.31 — ISOLAMENTO DE RASCUNHO POR USUÁRIO

- **Comando ou inspeção:** regressão `não restaura rascunho compartilhado apenas pela empresa` em `frontend/app/dashboard/pts/components/PtForm.test.tsx:396-414`; execução pré-correção `npm test -- --runInBand app/dashboard/pts/components/PtForm.test.tsx --no-cache --testNamePattern="não restaura rascunho compartilhado"`; correção em `frontend/app/dashboard/pts/components/PtForm.tsx:555-600`; repetição focused após a correção.
- **Evidência:** antes, o focused falhou com `expected document not to contain ... Rascunho de outro usuário`, porque a chave `gst.pt.wizard.draft.company-1` era restaurada por qualquer usuário da empresa. Depois, `draftOwnerScope` exige `company_id` e `user.id`, as chaves passam a ser `gst.pt.wizard.draft.<company>.<user>` e `compliancex.pt.wizard.draft.<company>.<user>`, e a chave compartilhada legada não é mais lida; o focused passou `1 suíte, 1 teste passado, 15 ignorados`.
- **Classificação:** `CONFIRMED` — exposição/confusão P1 de rascunho local entre usuários da mesma empresa corrigida; isso não substitui a limpeza de storage de sessão nem a autoridade server-side.
- **Ação tomada:** escopo mínimo da chave de autosave por identidade autenticada e fail-closed enquanto `company_id`/`user.id` ainda não estão disponíveis; nenhuma migração de dados local compartilhada, alteração de backend, APR, workflow ou produção.

## GATE FINAL-RECHECK-18 — FECHAMENTO APÓS ISOLAMENTO DE RASCUNHO

- **Comando ou inspeção:** focused `PtForm`; frontend `npm run test:ci`, `npm run lint`, `npx tsc --noEmit --pretty false`, build com `NEXT_PUBLIC_API_URL=https://api.sgsseguranca.com.br` e `NEXT_PUBLIC_APP_URL=https://app.sgsseguranca.com.br`; `npm audit --audit-level=high`; `git diff --check`; comparação de SHA; contagem de fronteira, EOL, sinks e listener local.
- **Evidência:** `PtForm` focused exit `0`, `1 suíte e 16/16 testes passados`; frontend `test:ci` exit `0`, `1 suíte skipped, 167 passed, 167 de 168` e `2 skipped, 915 passed, 917 total`. Lint exit `0` com `PERMISSION_IMPORTS_OK`/Stylelint, typecheck exit `0`, build Next `16.3.4` exit `0` com `91/91` páginas e audit `found 0 vulnerabilities`. Rechecagem: `APR_FILES=0`, `MIGRATION_FILES=1`, `WORKFLOW_FILES=0`, `VERIFY_NON_EOL_DIFF=0`, `PT_RUNTIME_DANGEROUS_SINKS=NONE` nos sinks perigosos auditados — o único `window.open` do PT recebe URL previamente validada por `safeExternalArtifactUrl` em `frontend/app/dashboard/pts/components/ChecklistSection.tsx:129-134` —, `NO_LISTENER_3100` e `HEAD=ORIGIN_MAIN=5a5ae66ae8eb6410ade8a4b0e68dbd3105fc6219` com `MAIN_DRIFT=NONE`; `git diff --check` exit `0`, com apenas os avisos CRLF históricos de `frontend/app/verify/page.tsx` e `page.test.tsx`.
- **Classificação:** `PASS` local condicionado aos comandos registrados abaixo; `STATUS=PARTIAL` global permanece por autenticação E2E real, banco/RLS/storage live, contratos backend externos e validação completa de foco/reduced-motion.
- **Ação tomada:** nenhuma publicação ou mudança remota; manter o worktree isolado para revisão.

## GATE 2.32 — INDICADOR DE RASCUNHO NA LISTAGEM

- **Comando ou inspeção:** regressão `não anuncia rascunho pertencente a outro usuário da mesma empresa` em `frontend/app/dashboard/pts/page.test.tsx:168-181`; execução pré-correção `npm test -- --runInBand app/dashboard/pts/page.test.tsx --no-cache --testNamePattern="não anuncia rascunho"`; correção em `frontend/app/dashboard/pts/page.tsx:42,163-180`; repetição focused após a correção.
- **Evidência:** antes, o teste falhou porque `PtsPage` detectava qualquer chave com prefixo `gst.pt.wizard.draft.` e renderizava o link `Retomar rascunho` para `user-2`. Depois, a página exige `user.id`/`company_id` e consulta somente as duas chaves da identidade atual; o focused passou `1 suíte, 1 teste passado`, sem warnings. O Gate 2.31 permanece a causa P1 única, agora coberta tanto na restauração quanto no indicador de listagem.
- **Classificação:** `CONFIRMED` — segunda superfície da mesma exposição/confusão P1 corrigida; não há confiança em prefixo global nem fallback de identidade ausente.
- **Ação tomada:** leitura mínima por chave escopada na listagem; nenhuma alteração de API, backend, APR, workflow ou produção.

## GATE 2.33 — INTEGRAÇÃO SOPHIE/PT COM RASCUNHO POR IDENTIDADE

- **Comando ou inspeção:** leitura de `frontend/src/lib/sophie-draft-storage.ts:58-121`, `frontend/app/dashboard/sst-agent/page.tsx:487-504` e do contrato de leitura em `frontend/app/dashboard/pts/components/PtForm.tsx:555-600,1435-1484`; regressão focused `npm test -- --runInBand src/lib/sophie-draft-storage.test.ts --no-cache --testNamePattern="persiste rascunhos"` antes/depois.
- **Evidência:** antes, o teste de contrato exigia o rascunho PT em `localStorage` na chave `gst.pt.wizard.draft.company-1.user-1`, mas a implementação escrevia em `sessionStorage` na chave somente da empresa; a execução red observou `ptDraft: null`. Depois, `storeSophiePtDraft` exige `companyId` e `userId`, escreve no `localStorage` com a chave escopada em `frontend/src/lib/sophie-draft-storage.ts:61,69-84,103-121`, e o agente passa ambas as identidades em `frontend/app/dashboard/sst-agent/page.tsx:497`. O focused pós-correção passou `1 suíte`, `1 teste passado`, `1 ignorado`; `PtForm` e `PtsPage` passam a consumir o mesmo contrato sem restaurar/anunciar rascunho de outro usuário.
- **Classificação:** `CONFIRMED` — inconsistência P1 de integridade do rascunho gerado pela Sophie corrigida; a implementação anterior podia não entregar o rascunho ao PT e usava uma chave ambígua entre usuários da mesma empresa.
- **Ação tomada:** alinhado o produtor Sophie ao contrato de autosave do PT e aplicado fail-closed quando empresa ou usuário não estão disponíveis; nenhuma credencial, chamada remota, alteração de APR, workflow ou produção.

## GATE FINAL-RECHECK-20 — FECHAMENTO APÓS INTEGRAÇÃO SOPHIE/PT

- **Comando ou inspeção:** focused de storage sensível, `PtForm` e `PtsPage`; frontend `npm run test:ci`, `npm run lint`, `$env:NODE_OPTIONS='--max-old-space-size=4096'; npx tsc --noEmit --pretty false`, build com `NEXT_PUBLIC_API_URL=https://api.sgsseguranca.com.br` e `NEXT_PUBLIC_APP_URL=https://app.sgsseguranca.com.br`, `npm audit --audit-level=high`; `git fetch origin`; `git diff --check`; comparação de SHA, fronteira de arquivos, APR/workflows/migrations, EOL e sinks.
- **Evidência:** focused passou `4 suítes`, `20 testes`; `test:ci` passou `168/169` suítes e `916/918` testes (`1` suíte e `2` testes ignorados). Lint passou com `PERMISSION_IMPORTS_OK` e Stylelint; typecheck exit `0`; build Next `16.3.4` compilou e gerou `91/91` páginas; audit retornou `found 0 vulnerabilities`. A rechecagem retornou `HEAD=5a5ae66ae8eb6410ade8a4b0e68dbd3105fc6219`, `ORIGIN_MAIN=cc4184625eac3f373481a4e2db6e499682d85314`, `MAIN_DRIFT=DETECTED` após avanço do `origin/main` por commits do trabalho APR, `STATUS_PATHS=54`, `APR_FILES=0`, `WORKFLOW_FILES=0`, `MIGRATION_FILES=1` (`1709000000403-harden-site-scope-fail-closed.ts`) e `VERIFY_NON_EOL_DIFF=0`. `git diff --check` exit `0`, emitindo somente os avisos CRLF históricos de `frontend/app/verify/page.tsx` e `page.test.tsx`; os sinks perigosos PT permanecem sem ocorrência não protegida e não houve listener local na porta 3100.
- **Classificação:** `PASS` nos checks automatizados locais e na fronteira de arquivos; `STATUS=PARTIAL` global permanece porque o banco/RLS/storage live, contratos server-side remotos, E2E autenticado, foco completo e `prefers-reduced-motion` não foram provados. O drift do `origin/main` é externo ao worktree e não foi incorporado para não interferir no APR concorrente.
- **Ação tomada:** nenhuma rebase, merge, commit, push, PR, deploy ou alteração remota; preservadas as mudanças concorrentes de APR no `origin/main` e os arquivos EOL-only de `verify`.

## GATE 2.34 — LIMPEZA DE RASCUNHOS LEGADOS NO LOGOUT

- **Comando ou inspeção:** regressão em `frontend/src/lib/browser-sensitive-storage.test.ts:3-53`; execução pré-correção `npm test -- --runInBand src/lib/browser-sensitive-storage.test.ts --no-cache --testNamePattern="remove caches"`; correção em `frontend/src/lib/browser-sensitive-storage.ts:36-51`; repetição focused após a correção.
- **Evidência:** antes, a regressão deixou `gst.pt.wizard.draft.company-legacy` e `gst.apr.wizard.draft.company-legacy` em `sessionStorage`, com `Received: "{}"`; depois, `clearSensitiveBrowserStorage` percorre `localStorage` e `sessionStorage` usando os mesmos prefixos sensíveis e o focused passou `1 suíte, 1 teste passado`. A suíte completa posterior passou `168/169` suítes e `916/918` testes.
- **Classificação:** `CONFIRMED` — resíduo P1 de dados de rascunho sensível após logout/troca de contexto corrigido no utilitário compartilhado; o caso PT legado não permanece disponível no storage do navegador após a limpeza.
- **Ação tomada:** ampliada somente a limpeza por prefixo para os dois storages do browser, mantendo chaves de tema e demais preferências não sensíveis; nenhum endpoint, banco, APR, workflow ou produção foi alterado.

## GATE FINAL-RECHECK-21 — FECHAMENTO APÓS LIMPEZA DE STORAGE

- **Comando ou inspeção:** focused de `browser-sensitive-storage`, Sophie/PT, `PtForm` e `PtsPage`; frontend `npm run test:ci`, `npm run lint`, `$env:NODE_OPTIONS='--max-old-space-size=4096'; npx tsc --noEmit --pretty false`, build com `NEXT_PUBLIC_API_URL=https://api.sgsseguranca.com.br` e `NEXT_PUBLIC_APP_URL=https://app.sgsseguranca.com.br`, `npm audit --audit-level=high`; `git diff --check`; comparação de SHA, fronteira de arquivos, APR/workflows/migrations, EOL e sinks.
- **Evidência:** focused final passou `4 suítes`, `20 testes`; `test:ci` passou `168/169` suítes e `916/918` testes (`1` suíte e `2` testes ignorados); lint passou com `PERMISSION_IMPORTS_OK` e Stylelint; typecheck exit `0`; build Next `16.3.4` gerou `91/91` páginas; audit retornou `found 0 vulnerabilities`. A inspeção residual manteve `NO_LISTENER_3100`, sinks PT apenas protegidos/gerenciados, `APR_FILES=0`, `WORKFLOW_FILES=0`, `MIGRATION_FILES=1` (`1709000000403-harden-site-scope-fail-closed.ts`) e `VERIFY_NON_EOL_DIFF=0`. `git diff --check` exit `0`, apenas com os avisos CRLF históricos de `frontend/app/verify/page.tsx` e `page.test.tsx`.
- **Classificação:** `PASS` nos checks automatizados locais e na fronteira PT; `STATUS=PARTIAL` global permanece por banco/RLS/storage live, contratos server-side remotos, E2E autenticado, foco completo e `prefers-reduced-motion`. O worktree continua baseado em `5a5ae66ae8eb6410ade8a4b0e68dbd3105fc6219`; o `origin/main` avançado para `cc4184625eac3f373481a4e2db6e499682d85314` não foi incorporado por conter o trabalho concorrente de APR.
- **Ação tomada:** nenhum commit, push, PR, merge, rebase ou deploy; preservada a worktree isolada para revisão.

## GATE 2.35 — COLISÃO DE TIMESTAMP DE MIGRATION COM APR

- **Comando ou inspeção:** `git fetch origin`; comparação de `git diff --name-only 5a5ae66ae8eb6410ade8a4b0e68dbd3105fc6219..origin/main`; inspeção de `git ls-tree -r --name-only origin/main backend/src/infra/database/migrations`; busca de referências da migration PT e dos testes de manifesto/compatibilidade.
- **Evidência:** `origin/main=cc4184625eac3f373481a4e2db6e499682d85314` contém `1709000000403-drop-redundant-apr-composite-indexes.ts` e `1709000000404-add-missing-apr-workflow-fk-constraints.ts`, enquanto o worktree PT havia criado `1709000000403-harden-site-scope-fail-closed.ts`. O overlap de arquivos com o upstream ficou restrito a `backend/src/shared/database/migration-history-compatibility.spec.ts`; nenhum arquivo APR foi modificado no worktree (`APR_FILES=0`).
- **Classificação:** `CONFIRMED` — colisão de integração entre migrations independentes, detectada antes de qualquer publicação; não é falha do runtime local.
- **Ação tomada:** migration PT renomeada para `backend/src/infra/database/migrations/1709000000405-harden-site-scope-fail-closed.ts`, classe e referências atualizadas; `npm test -- --runInBand src/modules/pts/pts-migrations.spec.ts src/shared/database/migration-history-compatibility.spec.ts --no-cache` passou `2 suítes / 27 testes`, e `npm run ci:migration:check` passou `323 files; 20 names derived from class`. O spec compartilhado ainda exigirá reconciliação ao integrar com o APR, sem rebase automático nesta worktree.

## GATE FINAL-RECHECK-22 — FECHAMENTO APÓS RECONCILIAÇÃO DE MIGRATION

- **Comando ou inspeção:** backend `npm run test:ci`, `npm run lint`, `npm run type-check`, `npm run build`, `npm audit --audit-level=high`, `npm run ci:migration:check`; revisão de status, `git diff --check`, drift, fronteira APR/workflows/migrations, EOL e sinks PT.
- **Evidência:** backend `test:ci` passou `328/328` suítes e `2878/2878` testes; lint, type-check e build terminaram com exit `0`; audit retornou `found 0 vulnerabilities`; manifesto passou `323 files; 20 names derived from class`. A migration PT atual é `1709000000405-harden-site-scope-fail-closed.ts`; o `origin/main` permanece em `cc4184625eac3f373481a4e2db6e499682d85314`, diferente do `HEAD=5a5ae66ae8eb6410ade8a4b0e68dbd3105fc6219` por commits APR; não houve arquivo APR ou workflow modificado.
- **Classificação:** `PASS` para o backend e o manifesto locais; `STATUS=PARTIAL` global permanece por banco/RLS live, storage live, contratos server-side remotos, E2E autenticado e validação completa no navegador. A colisão de timestamp não permanece no nome da migration PT, mas a reconciliação do spec compartilhado deve ocorrer numa integração futura controlada.
- **Ação tomada:** nenhuma rebase, merge, commit, push, PR ou deploy; preservada a separação do trabalho APR.

## GATE FINAL-RECHECK-19 — FECHAMENTO APÓS INDICADOR ESCOPADO

- **Comando ou inspeção:** focused `PtsPage`; frontend `npm run test:ci`, `npm run lint`, `npx tsc --noEmit --pretty false`, build com `NEXT_PUBLIC_API_URL=https://api.sgsseguranca.com.br` e `NEXT_PUBLIC_APP_URL=https://app.sgsseguranca.com.br`; `npm audit --audit-level=high`; `git diff --check`; comparação de SHA; contagem de fronteira, EOL, sinks e listener local.
- **Evidência:** focused `PtsPage` exit `0`, `1 suíte e 1 teste passado`; frontend `test:ci` exit `0`, `1 suíte skipped, 168 passed, 168 de 169` e `2 skipped, 916 passed, 918 total`. Lint exit `0` com `PERMISSION_IMPORTS_OK`/Stylelint, typecheck exit `0`, build Next `16.3.4` exit `0` com `91/91` páginas e audit `found 0 vulnerabilities`. Rechecagem: `STATUS_PATHS=51`, `APR_FILES=0`, `MIGRATION_FILES=1`, `WORKFLOW_FILES=0`, `VERIFY_NON_EOL_DIFF=0`, `PT_RUNTIME_DANGEROUS_SINKS=NONE` nos sinks perigosos auditados — o único `window.open` do PT está protegido por `safeExternalArtifactUrl` em `frontend/app/dashboard/pts/components/ChecklistSection.tsx:129-134` —, `NO_LISTENER_3100` e `HEAD=ORIGIN_MAIN=5a5ae66ae8eb6410ade8a4b0e68dbd3105fc6219` com `MAIN_DRIFT=NONE`; `git diff --check` exit `0`, com apenas os avisos CRLF históricos dos dois arquivos `verify`.
- **Classificação:** `PASS` local condicionado aos comandos registrados abaixo; `STATUS=PARTIAL` global permanece por autenticação E2E real, banco/RLS/storage live, contratos backend externos e validação completa de foco/reduced-motion.
- **Ação tomada:** nenhuma publicação ou mudança remota; worktree isolada preservada para revisão.

## GATE 3.7 — E2E PÚBLICO EM VIEWPORTS OPERACIONAIS

- **Comando ou inspeção:** tentativa oficial `npm run test:e2e:mobile`; tentativa de instalação dos browsers gerenciados com `npx playwright install chromium webkit`; execução da mesma suíte existente `frontend/e2e/public-mobile.spec.ts` com configuração temporária apontando para o Chrome instalado em `C:/Program Files/Google/Chrome/Application/chrome.exe` e `workers: 1`.
- **Evidência:** a execução oficial ficou `BLOCKED` antes de abrir páginas porque os executáveis Playwright Chromium/WebKit não estavam instalados; a instalação foi `BLOCKED` por timeout no CDN. Na repetição serial com o Chrome do sistema, `npx playwright test e2e/public-mobile.spec.ts --config=playwright.system-chrome.config.ts` terminou `24 passed (46.0s)`, cobrindo `mobile-320x568`, `mobile-360x800`, `mobile-390x844`, `mobile-430x932`, `desktop-1440x900`, `mobile-412x915`, `tablet-768x1024` e `landscape-844x390`. Foram validados overflow horizontal, tamanho/fonte de controles, login, recuperação de senha e redirecionamento de `/dashboard` sem sessão. A configuração temporária foi removida após a execução e não integra o diff.
- **Classificação:** `PASS` para o E2E público local com Chrome do sistema; `TEST_BLOCKER` permanece para o runner oficial com browsers gerenciados. Esta evidência não cobre autenticação real, mutações PT, banco/RLS, storage, foco completo de todos os modais ou `prefers-reduced-motion`.
- **Ação tomada:** nenhuma mudança funcional ou remota; apenas configuração efêmera de runner para reproduzir a suíte existente, removida ao final. Nenhum dado real foi usado.

## GATE FINAL-RECHECK-23 — FECHAMENTO APÓS E2E PÚBLICO

- **Comando ou inspeção:** `npm run test:e2e:mobile` (runner oficial); `npx playwright install chromium webkit`; repetição serial da suíte `e2e/public-mobile.spec.ts` com Chrome do sistema; verificação de `Test-Path frontend/playwright.system-chrome.config.ts`, listener TCP 3100, status, fronteira APR/workflows/migrations, `git diff --check`, EOL e sinks PT.
- **Evidência:** a suíte pública reproduzida terminou `24 passed (46.0s)` com exit `0`; não há listener na porta 3100 e a configuração temporária não existe mais. A execução oficial segue bloqueada por browsers gerenciados ausentes e timeout de download. Rechecagem mantém `APR_FILES=0`, `WORKFLOW_FILES=0`, `MIGRATION_FILES=1` (`1709000000405-harden-site-scope-fail-closed.ts`), `VERIFY_NON_EOL_DIFF=0`, `PT_RUNTIME_DANGEROUS_SINKS=NONE` e `git diff --check` exit `0`, com apenas os avisos históricos de CRLF dos dois arquivos `verify`.
- **Classificação:** `PASS` para checks locais e E2E público disponível; `STATUS=PARTIAL` global permanece por E2E autenticado do PT, banco/RLS/storage live, contratos server-side remotos, runner Playwright oficial e validação completa de foco/reduced-motion.
- **Ação tomada:** nenhum commit, push, PR, merge, rebase, deploy, migration compartilhada, chamada remota mutável ou alteração de produção; preservada a separação do APR.

## GATE 2.36 — INVALIDAÇÃO DE ESCOPO NA LISTA E NO STORAGE DO PT

- **Comando ou inspeção:** regressão `limpa dados em memória e recarrega a PT após troca de empresa` em `frontend/app/dashboard/pts/hooks/usePts.test.tsx:196-225`; regressão `limpa arquivos antigos e recarrega após trocar empresa` em `frontend/src/components/StoredFilesPanel.test.tsx:66-93`; focused red antes da correção com `npm test -- --runInBand app/dashboard/pts/hooks/usePts.test.tsx --no-cache --testNamePattern="limpa dados em memória"` e `npm test -- --runInBand src/components/StoredFilesPanel.test.tsx --no-cache`; inspeção de `selectedTenantStore`/`siteStore` e dos consumidores PT.
- **Evidência:** antes, a troca de empresa deixou `findPaginated` com somente `1` chamada quando eram esperadas `2`, mantendo `pt-company-a`; no painel de storage, a regressão falhou porque `PT a` permaneceu no DOM após a troca. Depois, `usePts` assina ambos os stores, incrementa a geração, invalida requests e limpa lista, métricas, revisões e modais em `frontend/app/dashboard/pts/hooks/usePts.ts:87-196`; loaders de overview/insights/regras rejeitam respostas tardias em `:239-287`. `StoredFilesPanel` faz a mesma invalidação e refetch em `frontend/src/components/StoredFilesPanel.tsx:139-185,228`. O focused pós-correção passou `6 suítes / 25 testes`; a suíte completa passou `169/170` suítes e `918/920` testes.
- **Classificação:** `CONFIRMED` — vazamento P0 de dados antigos em memória após troca de tenant/obra no PT corrigido nos dois consumidores auditados. `BACKEND_CONTRACT_BLOCKER` permanece para provar que API, RLS e storage rejeitam cross-tenant no servidor.
- **Contrato backend esperado/observado/risco:** esperado: toda resposta de PT/arquivo deve ser derivada do tenant e obra autenticados no servidor, mesmo que o browser envie filtros ou IDs antigos. Observado: a correção local impede a exibição de respostas antigas e reconsulta o escopo atual; backend/RLS/storage live não foram exercitados. Risco residual: migration, policy ou contrato remoto ausente ainda pode vazar dados independentemente da limpeza client-side.
- **Ação tomada:** adicionada invalidação por geração nos loaders PT e no painel compartilhado de arquivos, com limpeza de estados sensíveis e rejeição de respostas tardias; nenhuma API, migration, APR, workflow, produção ou storage remoto foi alterado.

## GATE FINAL-RECHECK-24 — CONSOLIDAÇÃO APÓS INVALIDAÇÃO DE ESCOPO

- **Comando ou inspeção:** focused PT/storage `6 suítes / 25 testes`; frontend `npm run test:ci`, `npm run lint`, `$env:NODE_OPTIONS='--max-old-space-size=4096'; npx tsc --noEmit --pretty false`, build com `NEXT_PUBLIC_API_URL=https://api.sgsseguranca.com.br` e `NEXT_PUBLIC_APP_URL=https://app.sgsseguranca.com.br` e `NODE_OPTIONS` de 4096 MB, `npm audit --audit-level=high`; `git diff --check`; status, listener 3100, EOL, sinks e fronteira APR/workflows.
- **Evidência:** focused `6/6` e `25/25`; `test:ci` `169/170` suítes e `918/920` testes, com `1` suíte e `2` testes ignorados; lint exit `0` com `PERMISSION_IMPORTS_OK`/Stylelint; typecheck exit `0`; build Next `16.3.4` gerou `91/91` páginas; audit `found 0 vulnerabilities`. O build sem `NODE_OPTIONS` foi repetido após OOM local e passou com 4096 MB. A configuração temporária do Playwright foi removida, `NO_LISTENER_3100`, `VERIFY_NON_EOL_DIFF=0`, `APR_FILES=0`, `WORKFLOW_FILES=0`, `MIGRATION_FILES=1` (`1709000000405-harden-site-scope-fail-closed.ts`) e `git diff --check` exit `0`, apenas com os avisos históricos de CRLF de `frontend/app/verify/page.tsx` e `page.test.tsx`.
- **Classificação:** `PASS` nos checks locais e no fechamento de invalidação client-side; `STATUS=PARTIAL` global permanece por E2E autenticado do PT, banco/RLS/storage live, contratos server-side remotos, browsers gerenciados do Playwright e foco/reduced-motion completos.
- **Ação tomada:** nenhuma publicação, rebase, merge, commit, push, PR, deploy, chamada remota mutável ou alteração de produção; APR preservado integralmente.

## GATE 2.37 — ALLOWLIST NO DOWNLOAD DE ARQUIVOS PT

- **Comando ou inspeção:** inspeção do download em `frontend/src/components/StoredFilesPanel.tsx:52-61,232-251`; busca dos sinks de URL e do utilitário `resolveSafeBrowserUrl`; regressão `bloqueia URL de arquivo não permitida antes de requisitar o blob` em `frontend/src/components/StoredFilesPanel.test.tsx:121-139`; focused `npm test -- --runInBand --no-cache --runTestsByPath src/components/StoredFilesPanel.test.tsx`.
- **Evidência:** antes, a regressão observou `mockApiGet` chamado com `https://evil.example/pt.pdf`, demonstrando que o caminho de download requisitava o blob sem allowlist. Depois, `fetchPdfBlob` resolve a URL por `resolveSafeBrowserUrl` antes de chamar o client autenticado; o focused passou `1 suíte, 3 testes`. O logger registrou somente o erro sanitizado esperado `URL bloqueada pela política de segurança`, sem requisição ao blob externo.
- **Classificação:** `CONFIRMED` — bypass P1 de allowlist no download de attachment corrigido. `BACKEND_CONTRACT_BLOCKER` permanece para autorização de usuário/tenant/ownership, expiração e conteúdo do recurso no servidor.
- **Contrato backend esperado/observado/risco:** esperado: o endpoint de acesso ao PDF deve autorizar o usuário e o tenant antes de emitir grant/URL, com expiração e MIME coerentes. Observado: a fronteira local impede destino externo não permitido antes do Bearer ser usado; backend e storage live não foram exercitados. Risco: uma URL HTTPS de origem permitida ainda exige autorização server-side correta.
- **Ação tomada:** validação centralizada da URL imediatamente antes da requisição do blob e regressão negativa; nenhuma API, storage, produção ou contrato remoto foi alterado.

## GATE 2.38 — FILTRO DE STORAGE COERENTE COM TENANT ATIVO

- **Comando ou inspeção:** inspeção do contrato em `backend/src/modules/pts/pts.controller.ts:282-300` e dos testes do controller que confirmam que `company_id` enviado pelo cliente é ignorado; inspeção de `frontend/app/dashboard/pts/page.tsx:280-287` e `frontend/src/components/StoredFilesPanel.tsx:101-132,467-488`; regressão `pode ocultar filtro de empresa quando o tenant é definido pelo contexto` em `frontend/src/components/StoredFilesPanel.test.tsx:104-119`; focused `npm test -- --runInBand --no-cache --runTestsByPath src/components/StoredFilesPanel.test.tsx app/dashboard/pts/page.test.tsx`.
- **Evidência:** o endpoint PT deriva o escopo do contexto confiável, enquanto a tela ainda carregava empresas e apresentava um seletor que não alterava o escopo autoritativo. Agora o painel aceita `showCompanyFilter`, a página PT usa `showCompanyFilter={false}` e não enumera empresas apenas para esse filtro; o focused passou `2 suítes, 4 testes` nos dois arquivos.
- **Classificação:** `CONFIRMED` — inconsistência P2 de UX/contrato corrigida; o controle que aparentava trocar empresa deixou de oferecer uma escolha sem efeito. Não altera a contagem de findings P2 de segurança; é uma correção complementar de coerência.
- **Contrato backend esperado/observado/risco:** esperado: listagem e bundle de arquivos PT derivarem empresa/obra do contexto autenticado, sem confiar em `company_id` do browser. Observado: controller e testes locais já implementam essa regra; QA/live não foi exercitado. Risco: mudança futura de contrato deve ser acompanhada por teste de integração, pois a UI não pode substituir autorização server-side.
- **Ação tomada:** removido o carregamento e filtro enganoso da página PT, mantendo o componente compartilhado compatível com módulos que possuem filtro autoritativo; nenhum backend ou módulo APR foi tocado.

## GATE FINAL-RECHECK-25 — CONSOLIDAÇÃO APÓS DOWNLOAD E STORAGE

- **Comando ou inspeção:** focused `StoredFilesPanel` e `PtsPage`; frontend `npm run test:ci`, `npm run lint`, `$env:NODE_OPTIONS='--max-old-space-size=8192'; npx tsc --noEmit --pretty false`, build com `$env:NEXT_PUBLIC_API_URL='https://api.sgsseguranca.com.br'`, `$env:NEXT_PUBLIC_APP_URL='https://app.sgsseguranca.com.br'` e `NODE_OPTIONS` de 8192 MB, `npm audit --audit-level=high`; fronteira com `git diff --check`, EOL de `verify`, APR/workflows e listener TCP 3100.
- **Evidência:** `StoredFilesPanel` passou `1/1` suíte e `3/3` testes; `PtsPage` passou `1/1` e `1/1`. A suíte frontend passou `169/170` suítes e `920/922` testes, com `1` suíte e `2` testes ignorados. Lint terminou com exit `0`, `PERMISSION_IMPORTS_OK` e Stylelint concluído; typecheck terminou com exit `0`; audit terminou com `found 0 vulnerabilities`. O build passou com Next `16.3.4`, compilação/typecheck e geração de `91/91` páginas; a tentativa anterior com heap menor teve OOM local no typecheck e foi repetida com heap ampliado. Rechecagem: `APR_FILES=0`, `WORKFLOW_FILES=0`, `VERIFY_NON_EOL_DIFF_EXIT=0`, `DIFF_CHECK_EXIT=0`, `DIFF_CHECK_MESSAGES=2` somente pelos avisos CRLF históricos de `frontend/app/verify/page.tsx` e `page.test.tsx`, `LISTENER_3100=NO`, `TEMP_CONFIG=False`, `HEAD=5a5ae66ae8eb6410ade8a4b0e68dbd3105fc6219` e `ORIGIN_MAIN=cc4184625eac3f373481a4e2dbd3106e499682d85314`.
- **Classificação:** `PASS` para os checks locais executáveis e para os dois incrementos frontend; `STATUS=PARTIAL` global permanece por E2E autenticado do PT, banco/RLS/storage live, contratos server-side remotos, browsers gerenciados do Playwright e validação completa de foco/reduced-motion.
- **Ação tomada:** nenhum commit, push, PR, merge, rebase, deploy, migration compartilhada, chamada remota mutável ou alteração de produção; worktree isolada e APR preservado.

## GATE FINAL-RECHECK-26 — REVALIDAÇÃO BACKEND E MIGRATIONS

- **Comando ou inspeção:** no backend, `npm run test:ci`, `npm run lint`, `npm run type-check`, `npm run build`, `npm audit --audit-level=high` e `npm run ci:migration:check`; inspeção final de que nenhuma alteração de backend ocorreu depois da validação funcional PT já registrada, além da separação de QA/live e produção.
- **Evidência:** `test:ci` terminou com exit `0`, `328/328` suítes e `2878/2878` testes; lint e typecheck terminaram com exit `0`; build Nest terminou com exit `0`; audit terminou com `found 0 vulnerabilities`; manifesto de migrations passou com `323 files; 20 names derived from class`. Nenhuma migration foi executada em banco compartilhado e nenhum endpoint mutável foi chamado.
- **Classificação:** `PASS` para a validação local de backend, contratos PT testados e integridade do manifesto; `BACKEND_CONTRACT_BLOCKER` permanece para execução da migration/RLS e validação de autorização, storage e isolamento em QA autenticado/live.
- **Contrato backend esperado/observado/risco:** esperado: tenant, site, ownership, role, estado, storage e auditoria sejam aplicados pela autoridade server-side e pelas policies do banco em runtime. Observado: código, testes e manifesto locais verdes; banco remoto, RLS live, storage e credenciais autenticadas não foram exercitados. Risco: um ambiente implantado divergente pode não refletir o código/migration auditado.
- **Ação tomada:** nenhuma alteração adicional; somente revalidação. Não houve commit, push, PR, merge, deploy, migration compartilhada ou produção tocada.

## GATE FINAL-RECHECK-27 — DRIFT DA MAIN E FRONTEIRA APR

- **Comando ou inspeção:** `git fetch origin`; `git rev-parse HEAD`; `git rev-parse origin/main`; `git log --oneline HEAD..origin/main`; inspeção final de `git status --short --branch`, fronteira de caminhos e ausência de merge/rebase.
- **Evidência:** `AUDIT_START_MAIN_SHA=5a5ae66ae8eb6410ade8a4b0e68dbd3105fc6219`; `AUDIT_END_MAIN_SHA=cc4184625eac3f373481a4e2dbd3106e499682d85314`; `HEAD` permaneceu no SHA inicial. Os commits posteriores em `origin/main` são `bbf77034`, `e4c0d0d7`, `58d96b8a`, `22ca846e`, `481f8504`, `d3241217` e o merge `cc418462`, identificados como trabalho APR/infra associada. `APR_FILES=0` e `WORKFLOW_FILES=0`; não houve incorporação do drift.
- **Classificação:** `PASS` de isolamento da branch e de não interferência no APR; `STATUS=PARTIAL` global permanece porque a branch auditada não contém o drift posterior e a reconciliação futura deve ser feita com revisão própria, sem rebase cego.
- **Ação tomada:** somente inspeção e registro dos SHAs; nenhum merge, rebase, cherry-pick, commit, push, PR, deploy ou alteração no branch compartilhado.

## GATE 3.8 — E2E AUTENTICADO SINTÉTICO DO PT

- **Comando ou inspeção:** smoke `frontend/e2e/pt-authenticated-visual.spec.ts:56-169` com APIs interceptadas exclusivamente no navegador; execução inicial com Chrome do sistema em `390x844`, que falhou com `Expected: >= 44 / Received: 42` em `e2e/pt-authenticated-visual.spec.ts:146`; correção em `frontend/app/dashboard/pts/components/PtsFilters.tsx:16` e `frontend/src/components/StoredFilesPanel.tsx:104`; repetição nos projetos `pt-mobile-390x844`, `pt-tablet-768x1024` e `pt-desktop-1440x900`.
- **Evidência:** o teste sintético autentica somente uma sessão fictícia, intercepta `/auth/*` e `/pts*`, e deixa os assets/Next reais. Depois da correção `min-h-11 text-base sm:text-sm`, `npx playwright test e2e/pt-authenticated-visual.spec.ts --config=playwright.system-chrome.config.ts` terminou `6 passed (23.8s)`. Foram verificadas ausência de overflow, controles com ao menos `44px`, fonte móvel de `16px`, foco inicial do modal de reprovação e restauração de foco após Escape nos três viewports.
- **Classificação:** `CONFIRMED` — finding P2 de alvo tátil/tipografia dos filtros do PT corrigido. O teste é evidência de UI local, não de autenticação, RBAC, RLS, storage ou contrato remoto.
- **Ação tomada:** normalizados altura mínima e tipografia responsiva dos filtros PT e do painel de arquivos compartilhado; adicionada cobertura browser sintética autenticada. A configuração de Chrome do sistema foi temporária e removida após a execução; nenhum dado real ou endpoint remoto foi usado.

## GATE FINAL-RECHECK-28 — FECHAMENTO APÓS QA VISUAL SINTÉTICO

- **Comando ou inspeção:** `npm run test:ci`, `npm run lint`, `npx tsc --noEmit --pretty false`, build com `NODE_OPTIONS=--max-old-space-size=8192` e variáveis públicas explícitas, `npm audit --audit-level=high`; E2E sintético PT em três viewports; `git diff --check`, EOL, sinks, listener e fronteira APR/workflows.
- **Evidência:** frontend `169/170` suítes e `920/922` testes, com `1` suíte e `2` testes ignorados; lint/typecheck/build/audit verdes, build Next `16.3.4` com `91/91` páginas; E2E PT sintético `6/6` testes; E2E público anterior `24 passed`. `DIFF_CHECK_EXIT=0` com apenas os dois avisos CRLF históricos de `verify`, `VERIFY_NON_EOL_DIFF_EXIT=0`, `APR_FILES=0`, `WORKFLOW_FILES=0`, `TEMP_CONFIG=False`, `LISTENER_3100=NO`, e sink `window.open` do PT mantido atrás de `safeExternalArtifactUrl` em `frontend/app/dashboard/pts/components/ChecklistSection.tsx:129-134`.
- **Classificação:** `PASS` nos checks locais e na cobertura visual sintética disponível; `STATUS=PARTIAL` global permanece por E2E autenticado real, banco/RLS/storage live, contratos server-side remotos, runner Playwright oficial e validação completa de reduced-motion.
- **Ação tomada:** nenhum commit, push, PR, merge, rebase, deploy, migration compartilhada, chamada remota mutável ou alteração de produção.

## GATE 3.9 — REDUCED MOTION NO PT

- **Comando ou inspeção:** execução do teste `respeita prefers-reduced-motion no shell do PT` em `frontend/e2e/pt-authenticated-visual.spec.ts:177-195`, com `page.emulateMedia({ reducedMotion: 'reduce' })`, usando Chrome do sistema nos projetos `pt-mobile-390x844`, `pt-tablet-768x1024` e `pt-desktop-1440x900`.
- **Evidência:** o teste verificou `scroll-behavior: auto` no documento e duração de transição de no máximo `0.1ms` no filtro de pesquisa; o caso passou nos três viewports. O smoke completo do arquivo terminou `9 passed (29.9s)`. A regra global correspondente está em `frontend/app/globals.css:2978-2987`.
- **Classificação:** `PASS` para o shell PT e os componentes exercitados sob reduced motion; a evidência não generaliza para módulos fora do escopo PT.
- **Ação tomada:** nenhuma alteração adicional na política global; adicionada prova browser específica do PT.

## GATE FINAL-RECHECK-29 — FECHAMENTO FINAL DA EXECUÇÃO LOCAL

- **Comando ou inspeção:** rechecagem do estado da worktree, EOL de `verify`, fronteira APR/workflows, listener local, sinks PT e consistência dos relatórios após o smoke browser autenticado sintético.
- **Evidência:** smoke PT `9/9` passou; `APR_FILES=0`, `WORKFLOW_FILES=0`, `TEMP_CONFIG=False`, `LISTENER_3100=NO`, `VERIFY_NON_EOL_DIFF_EXIT=0` e `DIFF_CHECK_EXIT=0`, com exatamente dois avisos históricos de CRLF em `frontend/app/verify/page.tsx` e `page.test.tsx`. O único `window.open` em runtime PT permanece validado por `safeExternalArtifactUrl` em `frontend/app/dashboard/pts/components/ChecklistSection.tsx:129-134`.
- **Classificação:** `PASS` local para código, testes, navegador sintético e fronteira; `STATUS=PARTIAL` global permanece por autenticação real, QA/RLS/storage live, contratos server-side remotos e runner Playwright oficial.
- **Ação tomada:** nenhum commit, push, PR, merge, rebase, deploy, migration compartilhada, chamada remota mutável ou alteração de produção; o worktree permanece isolado para revisão.

## GATE 3.10 — FORMULÁRIO PT: ALVOS DE TOQUE E ZOOM MÓVEL

- **Comando ou inspeção:** execução do teste autenticado sintético `mantém os controles do formulário PT acessíveis no celular` em `frontend/e2e/pt-authenticated-visual.spec.ts:261-290`, com sessão fictícia, APIs interceptadas no browser e Chrome do sistema em `390x844`, `768x1024` e `1440x900`; repetição red/green após a inspeção do formulário `/dashboard/pts/new?field=1`.
- **Evidência:** antes da correção, o teste falhou com `{"tag":"input","id":"pt-numero","name":"numero","type":null,"fontSize":"14px"}` e `Expected: >= 44 / Received: 42`. Depois, o shell do formulário recebeu a classe `ds-pt-form-page` em `frontend/app/dashboard/pts/components/PtForm.tsx:1973`, a regra base de `min-height: 2.75rem` e a regra móvel de `font-size: 1rem` em `frontend/app/globals.css:704-715`; `npx playwright test e2e/pt-authenticated-visual.spec.ts --config=playwright.system-chrome.config.ts` terminou `12 passed (27.1s)`.
- **Classificação:** `CONFIRMED` — finding P2 de usabilidade/acessibilidade móvel corrigido; a fonte de 16 px evita zoom automático em iOS e o alvo mínimo de 44 px vale para os controles textuais do formulário PT em todos os viewports exercitados.
- **Ação tomada:** aplicada menor correção estrutural scoped ao PT, sem alterar router, state manager ou biblioteca visual; adicionada regressão browser para overflow, controles, foco e reduced-motion. O runner temporário e os dados de sessão foram locais/sintéticos; nenhum endpoint remoto, credencial real ou produção foi usado.

## GATE FINAL-RECHECK-30 — FECHAMENTO APÓS FORMULÁRIO PT

- **Comando ou inspeção:** `npm run lint`; `$env:NODE_OPTIONS='--max-old-space-size=8192'; npx tsc --noEmit --pretty false`; `npm run test:ci`; `npm run build` com `NODE_OPTIONS=--max-old-space-size=8192`, `NEXT_PUBLIC_API_URL=https://api.sgsseguranca.com.br` e `NEXT_PUBLIC_APP_URL=https://app.sgsseguranca.com.br`; `npm audit --audit-level=high`; E2E sintético PT em três viewports; `git diff --check`; status, listener TCP 3100, EOL e fronteira APR/workflows.
- **Evidência:** lint exit `0` com `PERMISSION_IMPORTS_OK` e Stylelint concluído; typecheck exit `0`; `test:ci` exit `0`, `1 suíte skipped, 169 passed, 169 de 170 total`, `2 skipped, 920 passed, 922 total`; build Next `16.3.4` exit `0` com `91/91` páginas; audit exit `0`, `found 0 vulnerabilities`; E2E autenticado sintético exit `0`, `12 passed (27.1s)`. Rechecagem mantém `APR_FILES=0`, `WORKFLOW_FILES=0`, `VERIFY_NON_EOL_DIFF_EXIT=0`, listener TCP `3100=NO` e nenhum runner temporário no worktree; `git diff --check` exit `0` com três avisos de normalização de EOL: `frontend/app/globals.css` e os dois arquivos históricos `frontend/app/verify/page.tsx` e `frontend/app/verify/page.test.tsx`.
- **Classificação:** `PASS` para código, regressões, build, dependências e browser sintético locais; `STATUS=PARTIAL` global permanece por autenticação real, QA/RLS/storage live, confirmação do deploy remoto, browsers gerenciados do Playwright e contratos server-side externos.
- **Ação tomada:** nenhum commit, push, PR, merge, rebase, deploy, migration compartilhada, chamada remota mutável ou alteração de produção; o worktree e a separação do APR permanecem preservados.

## GATE FINAL-RECHECK-31 — DISPONIBILIDADE DO QA REMOTO

- **Comando ou inspeção:** `Invoke-WebRequest -Uri https://api-loadtest.sgsseguranca.com.br/health -Method Get -TimeoutSec 15`; `Resolve-DnsName api-loadtest.sgsseguranca.com.br`; `Test-NetConnection api-loadtest.sgsseguranca.com.br -Port 443 -InformationLevel Detailed`.
- **Evidência:** DNS resolveu `api-loadtest.sgsseguranca.com.br` para `83.229.115.37`; a requisição `/health` terminou em `System.Threading.Tasks.TaskCanceledException` após 15 segundos; `TCP_TEST=False` e `PING=False` para a porta 443. Nenhum token, credencial ou endpoint mutável foi usado.
- **Classificação:** `EVIDENCE_BLOCKED` — o runtime QA não está alcançável para E2E autenticado, banco/RLS, storage ou validação de contrato remoto.
- **Contrato esperado/observado/risco:** esperado: endpoint de saúde QA responder em HTTPS e permitir sessão de teste controlada para validar PT; observado: DNS existe, mas HTTPS/ICMP não responde no momento; risco: não é possível provar que o código/migration auditado está implantado nem que isolamento, autorização e storage funcionam no ambiente operacional.
- **Ação tomada:** nenhuma alteração remota; produção não foi acessada; bloqueio mantido explícito para o relatório final.

## GATE 3.11 — FOCUS URL ALLOWLIST / SELETOR DOM

- **Comando ou inspeção:** inspeção de `frontend/app/dashboard/pts/components/PtForm.tsx:467,584-586` e `frontend/app/dashboard/pts/components/pt-approval-focus.ts:1-5`; regressão `ignora um destino de foco inválido vindo da URL` em `frontend/app/dashboard/pts/components/PtForm.test.tsx`; focused red/green `npm test -- --runInBand --no-cache --runTestsByPath app/dashboard/pts/components/PtForm.test.tsx`.
- **Evidência:** antes da correção, o teste falhou com `SyntaxError: '[data-pt-focus-target=""],,' is not a valid selector` em `PtForm.tsx:584`, porque o valor arbitrário de `focus` era interpolado no `querySelector`. Depois, `isPtFocusTarget` aceita somente `basic-info`, `risk-analysis`, `checklists` e `team` em `frontend/app/dashboard/pts/components/pt-approval-focus.ts:7-15`, e `PtForm` ignora valores inválidos antes do seletor em `frontend/app/dashboard/pts/components/PtForm.tsx:51-54,467-471`; o focused pós-correção passou `1 suíte, 17 testes`.
- **Classificação:** `CONFIRMED` — finding P2 de robustez e injeção de seletor DOM por parâmetro de URL corrigido; não classificado como XSS porque o sink não injeta HTML/script e o impacto reproduzido era a exceção assíncrona do wizard.
- **Ação tomada:** adicionada allowlist tipada no helper já existente e teste de regressão negativo; nenhum endpoint, backend, migration, storage, APR, produção ou ambiente remoto foi alterado.

## GATE FINAL-RECHECK-32 — CONSOLIDAÇÃO APÓS ALLOWLIST DE FOCO

- **Comando ou inspeção:** focused `PtForm.test.tsx` pós-correção; `npm run lint`; `$env:NODE_OPTIONS='--max-old-space-size=8192'; npx tsc --noEmit --pretty false`; `npm run test:ci`; `npm run build` com `NODE_OPTIONS=--max-old-space-size=8192`, `NEXT_PUBLIC_API_URL=https://api.sgsseguranca.com.br` e `NEXT_PUBLIC_APP_URL=https://app.sgsseguranca.com.br`; `npm audit --audit-level=high`; `npx playwright test e2e/pt-authenticated-visual.spec.ts --config=playwright.system-chrome.config.ts` com as variáveis públicas explícitas; `git diff --check`; status, EOL, fronteira APR/workflows e disponibilidade QA.
- **Evidência:** focused `PtForm` passou `1/1` suíte e `17/17` testes; lint exit `0` com `PERMISSION_IMPORTS_OK` e Stylelint concluído; typecheck exit `0`; `test:ci` exit `0`, `1 suíte skipped, 169 passed, 169 de 170 total` e `2 skipped, 921 passed, 923 total`; build Next `16.3.4` exit `0` com `91/91` páginas; audit exit `0`, `found 0 vulnerabilities`; E2E sintético PT exit `0`, `12 passed (36.8s)` em `390x844`, `768x1024` e `1440x900`. A primeira tentativa do E2E sem `NEXT_PUBLIC_API_URL` foi `12 failed` porque o app usou o fallback local `127.0.0.1:3011` e a fixture não autenticou; após fornecer a URL pública explícita à execução local, a repetição passou. A configuração temporária do Chrome do sistema foi removida após o teste.
- **Classificação:** `PASS` nos checks locais, regressão negativa, build e browser sintético; `STATUS=PARTIAL` global permanece por autenticação real, QA/RLS/storage live, confirmação de deploy remoto, browsers gerenciados do Playwright e contratos server-side externos. O QA segue bloqueado por `GATE FINAL-RECHECK-31`.
- **Ação tomada:** correção local, teste de regressão e revalidação completa; nenhuma alteração remota, credencial, chamada mutável, commit, push, PR, merge, rebase, deploy ou migration compartilhada.

## GATE FINAL-RECHECK-33 — CODEFACTOR E REVALIDAÇÃO DO FIXTURE E2E

- **Comando ou inspeção:** inspeção do check CodeFactor no PR após o primeiro push; leitura read-only do relatório do check; refatoração de `backend/src/modules/pts/pts.service.ts:1192-1351` e `frontend/e2e/pt-authenticated-visual.spec.ts:49-191`; focused backend `npm test -- --runInBand src/modules/pts/pts.service.spec.ts`; target lint backend `npx eslint src/modules/pts/pts.service.ts --max-warnings=0`; E2E sintético com Chrome do sistema, variáveis públicas explícitas e três viewports.
- **Evidência:** CodeFactor inicialmente apontou somente dois métodos complexos introduzidos pelo diff: `PtsService.update` (complexidade 22) e o handler inline do fixture PT (complexidade 16). O update foi decomposto em helpers privados de escopo/site/datas/aplicação, mantendo o contrato; o fixture foi convertido para tabela declarativa com resolvedor por método e wildcard. A primeira execução após a conversão falhou `12/12` no login porque o resolvedor não consultava a chave `*:/auth/...`; a correção em `frontend/e2e/pt-authenticated-visual.spec.ts:164-170` passou `12/12` (`39.7s`). O teste backend passou `1 suíte, 80 testes`; o ESLint alvo passou exit `0`.
- **Classificação:** `CONFIRMED` — apontamentos de complexidade do CodeFactor tratados sem mudança funcional pretendida; regressão do próprio fixture detectada e corrigida. `STATUS=PARTIAL` operacional permanece para o check remoto final, QA autenticado real, RLS/storage live e contratos server-side.
- **Ação tomada:** alteração mínima e localizada no backend PT e no fixture E2E; configuração temporária do Chrome removida após execução. Nenhuma alteração em produção, VPS, migration compartilhada ou workflow foi feita; os quatro arquivos preexistentes fora do escopo permanecem não staged.

## GATE FINAL-RECHECK-34 — BATERIA PRÉ-COMMIT

- **Comando ou inspeção:** backend `npm run lint`, `npm run test:ci`, `npm run type-check`, `npm run build`, `npm run ci:migration:check`, `npm audit --audit-level=high`; frontend `npm run lint`, `npx tsc --noEmit --pretty false` com heap de 8192 MB, `npm run test:ci`, `npm run build` com `NEXT_PUBLIC_API_URL` e `NEXT_PUBLIC_APP_URL` públicos explícitos, `npm audit --audit-level=high`; inspeção `git diff --check` e fronteira de arquivos.
- **Evidência:** backend lint, type-check e build terminaram exit `0`; `test:ci` passou `328/328` suítes e `2878/2878` testes; manifesto passou `323 files; 20 names derived from class`; audit reportou `found 0 vulnerabilities`. Frontend lint terminou exit `0` com `PERMISSION_IMPORTS_OK` e Stylelint; type-check terminou exit `0`; `test:ci` passou `169/170` suítes e `921/923` testes, com `1` suíte e `2` testes ignorados; build Next `16.3.4` compilou e gerou `91/91` páginas; audit reportou `found 0 vulnerabilities`. E2E PT sintético pós-correção passou `12/12` em `39.7s`. `git diff --check` não encontrou erro; os únicos avisos são a normalização CRLF histórica dos dois `frontend/app/verify/*` fora do escopo.
- **Classificação:** `PASS` para a bateria local e o escopo da mudança; `STATUS=PARTIAL` somente para evidências que dependem de ambiente externo (QA autenticado real, RLS/storage live, contratos server-side remotos e checks do PR).
- **Ação tomada:** alterações CodeFactor serão commitadas em causas atômicas separadas; nenhuma alteração em APR, workflow, produção ou VPS. Os quatro arquivos preexistentes fora do escopo permanecem não staged.
